# LinkX 管理端开发文档

> 面向运营、审核、风控和系统管理场景的后台管理端建设说明

本文档用于指导 LinkX 管理端的规划、设计与实施。仓库已具备较完整的客户端、后端与管理端能力；**M1–M4 已交付**，当前重点是 M5（联调验收、生产权限校验、风控与体验完善）。后文中的初始化方案、接口草案与建表 SQL 主要作为设计依据与对照清单，**不要再按「从零新建管理端」推进**。


### 进度图例（对照代码盘点）

| 标记 | 含义 |
|---|---|
| ✅ | 已实现（前后端或等效能力已落地） |
| ⚠️ | 部分实现 / 等效实现（形态与文档草案有差异，或能力不完整） |
| ❌ | 未实现 / 仍属暂缓 |
| 🔄 | 进行中（M5 验收与完善） |

盘点基准：`linkx-admin` 路由/页面/API + `linkx-server` `/admin/**` Controller 与 Flyway `V5`–`V32`。

---

## 1. 建设目标

管理端的核心目标不是替代客户端，而是为平台运营和治理提供能力，包括：

- ✅ 用户与账号管理
- ✅ 内容审核与违规处置
- ✅ 角色权限与菜单管理
- ✅ 系统配置与运营配置
- ✅ 审计日志与安全风控
- ✅ 数据统计与业务监控
- ✅ 版本、公告、反馈、举报等运营能力（版本配置并入系统配置；举报走审核/风险流程）

---

## 2. 当前项目现状判断

### 2.1 客户端现状

当前 `linkx-client` 已覆盖大量终端用户功能，包含：

- 登录、注册、找回密码、双 Token 登录态
- 单聊、群聊、消息历史、消息编辑/撤回/转发
- 好友、群组、群通知、联系人资料
- 朋友圈、日历、笔记、收藏
- 文件、网盘、分享、预览
- 红包、余额、通知、反馈
- 会议、语音/视频通话
- Electron 桌面能力与设置体系

结论是：客户端已接近完整产品态；管理端并行建设已完成第一版闭环。

### 2.2 后端现状

当前 `linkx-server` 已具备较强的后台数据基础，并已落地独立 `/admin/**` 管理端模块，包含：

- 既有业务基础：`RbacController`、`RbacService`、`SysUser`、`SysRole`、`SysPermission`
- `AuditLogService`、`LoginAuditService`、`DeviceSessionService`、`PresenceService`
- `ComplianceController`、`SensitiveWordService`、`FeedbackController`、`VersionController`
- 管理端控制器与服务：`AdminAuthController`、`AdminUserController`、`AdminRoleController`、`AdminMenuController`、`AdminDashboardController`、`AdminFeedbackController`、`AdminReviewController`、`AdminRiskEventController`、`AdminNoticeController`、`AdminSettingController`、`AdminStatisticsController` 等
- Flyway 迁移已覆盖管理端菜单、权限、公告、审核、风控、Banner、设备、TOTP、角色种子等（见 `V5` 及后续迁移）

结论是：后端管理端能力已可支撑运营、审核、安全与只读等角色场景，后续以增强与验收为主。

### 2.3 管理端现状

**M1–M4 已交付。** 仓库已存在独立前端工程 `linkx-admin`（Vue 3 + TypeScript + Vite + Pinia + Vue Router + Naive UI），与客户端 UI/状态/路由隔离，并通过 `/api` 代理对接 `linkx-server`。

当前已落地能力（摘要）：

- 登录鉴权、Token 刷新、权限守卫、动态菜单、国际化
- 仪表盘、用户列表/详情、角色/权限/菜单管理
- 操作日志、登录日志、风险事件
- 反馈管理、内容审核、敏感词、黑名单、设备管理
- 公告与通知收件箱、Banner、系统配置（含客户端版本配置）
- 统计分析、导出与批量处置、管理员 TOTP 等

刻意调整：独立「版本管理」菜单已并入「系统配置 → 客户端配置」（见迁移 `V10`），路由 `/admin/versions` 重定向到设置页。

当前阶段（M5）建议聚焦：

- 全链路联调与按角色验收（超管 / 运营 / 审核 / 安全 / 只读）
- 生产权限校验与安全加固
- 风控、审计与体验完善（如强更/灰度产品化、菜单排序等）

---

## 3. 建议的管理端工程形态

独立前端工程已落地，目录形态如下：

```text
LinkX/
├── linkx-client/        # 终端用户桌面客户端
├── linkx-server/        # Spring Boot 后端
└── linkx-admin/         # 管理端前端（已交付）
```

### 3.1 技术建议

`linkx-admin` 已采用以下技术栈（与规划一致）：

- ✅ Vue 3
- ✅ TypeScript
- ✅ Vite
- ✅ Pinia
- ✅ Vue Router
- ✅ Naive UI
- ✅ ECharts
- ✅ Axios 封装统一请求层

与客户端同属 Vue 3 生态，便于团队维护；后续增强沿用现有栈即可。

### 3.2 部署建议

管理端建议作为独立 Web 应用部署，不必依赖 Electron：

- 生产环境单独域名，例如 `admin.xxx.com`
- 通过 API 网关或后端统一接口访问
- 与客户端共享后端接口，但权限隔离

---

## 4. 角色与权限模型

管理端应基于 RBAC 模型进行控制。

### 4.1 角色建议

- ✅ 超级管理员：系统全权限（`admin` / `super_admin`）
- ✅ 运营管理员：内容、公告、版本、反馈、统计（种子角色 `ops_admin`）
- ✅ 审核管理员：敏感词、举报、封禁、违规内容处置（种子角色 `audit_admin`）
- ✅ 安全管理员：登录审计、设备管理、风控策略（种子角色 `security_admin`）
- ✅ 只读观察员：只读查看数据和报表（种子角色 `readonly_observer`）

### 4.2 权限维度

建议按以下维度拆分权限：

- ✅ 页面权限：是否可进入某个菜单
- ✅ 按钮权限：新增、编辑、删除、导出、封禁、审核等
- ⚠️ 数据权限：仅本人、部门、全部、指定组织（已有 `@DataScope` 基础接入，部门/组织级未产品化）
- ✅ 操作审计：关键动作必须记录日志

### 4.3 后端复用点

可优先复用（已复用）：

- ✅ `SysUser`
- ✅ `SysRole`
- ✅ `SysPermission`
- ✅ `SysRolePermission`
- ✅ `SysUserRole`
- ✅ `RbacController` / `RbacService`
- ✅ `AuditLogService`
- ✅ `LoginAuditService`

---

## 5. 核心功能模块规划

## 5.1 概览仪表盘

用于展示平台整体运行态势：

- ✅ 注册用户数（`totalUsers`）
- ⚠️ 日活 / 周活 / 月活（仪表盘提供 `activeUsers`，非完整 DAU/WAU/MAU 拆分）
- ⚠️ 今日消息量（趋势/统计模块覆盖消息量，摘要卡片未单独展示）
- ✅ 在线设备数
- ⚠️ 举报待处理数量（并入待审核/风险待办）
- ✅ 反馈待处理数量
- ⚠️ 敏感词命中次数（统计页有敏感词趋势）
- ⚠️ 风控拦截次数（统计页有风控趋势；摘要含 `riskEvents`）

### 页面建议

- ✅ 总览卡片
- ✅ 趋势图
- ✅ 实时在线面板
- ✅ 待处理工单列表

---

## 5.2 用户管理

管理员可以查看、搜索、编辑和处置用户。

### 功能

- ✅ 用户列表
- ✅ 用户详情
- ✅ 修改资料
- ✅ 重置密码
- ✅ 冻结 / 解冻账号
- ✅ 封禁 / 解封
- ✅ 设备列表查看
- ✅ 登录记录查看
- ✅ 黑名单管理
- ✅ 导出用户数据

### 相关后端能力

- ✅ `UserController`（管理端主要走 `AdminUserController`）
- ✅ `SysUserService`
- ✅ `DeviceSessionService`
- ✅ `LoginAuditService`
- ✅ `UserPreferenceService`
- ✅ `ComplianceController`

---

## 5.3 内容审核

用于审核平台内存在风险或违规的内容。

### 可审核对象

- ✅ 朋友圈动态（`moment`）
- ✅ 评论（`moment_comment`）
- ⚠️ 群公告（审核任务模型可扩展，当前主路径为朋友圈/评论）
- ❌ 群文件
- ❌ 收藏内容
- ✅ 用户反馈（独立反馈模块）
- ⚠️ 举报内容（走审核/风险事件）
- ✅ 敏感词命中内容（敏感词管理 + 审核/风控）

### 功能

- ✅ 违规内容列表
- ✅ 审核通过 / 驳回
- ⚠️ 删除内容（驳回处置为主，非独立删内容入口）
- ⚠️ 标记风险等级（风险事件侧有等级/处置）
- ✅ 记录审核意见
- ✅ 批量处置

### 相关后端能力

- `SensitiveWordService`
- `ComplianceController`
- `FeedbackController`
- `AuditLogService`

---

## 5.4 角色与权限管理

用于管理平台的后台角色、菜单和接口权限。

### 功能

- ✅ 角色列表
- ✅ 新增 / 编辑 / 删除角色
- ⚠️ 绑定权限（角色绑定菜单已落地；独立 `roles/{id}/permissions` 未做）
- ✅ 绑定用户
- ✅ 权限树管理（权限点 CRUD）
- ⚠️ 菜单管理（后端 CRUD 已有；前端目前只读树列表）
- ✅ 按钮权限管理

### 备注

如果当前后端 RBAC 只做到基础权限控制，建议补充：

- ✅ 菜单表（`sys_admin_menu`）
- ✅ 接口权限标识
- ✅ 按钮权限编码
- ⚠️ 数据权限范围（`@DataScope` 基础有，部门/组织未产品化）

---

## 5.5 系统配置

用于管理平台的全局运行参数。

### 可配置项示例

- ✅ 注册开关
- ✅ 验证码开关
- ✅ 登录风控策略
- ⚠️ 敏感词开关（敏感词库可管）
- ⚠️ 文件上传限制（部分在运行配置/业务侧）
- ✅ 版本信息（系统配置 → 客户端）
- ⚠️ 客服联系方式
- ✅ 公告配置（公告管理模块）
- ⚠️ 反馈处理规则（状态流转已有，规则引擎未做）

### 相关后端能力

- `LinkxProperties`
- `VersionController`
- `Application.yml`
- 配置中心或数据库配置表

---

## 5.6 审计与日志

用于追踪关键操作，满足安全与合规要求。

### 功能

- ✅ 操作日志查询
- ✅ 登录日志查询
- ⚠️ 异常访问记录（风险事件 / 登录失败覆盖部分）
- ✅ 设备登录记录（设备管理 + 用户登录记录）
- ✅ 管理员操作链路追踪（操作日志）
- ✅ 导出日志

### 建议记录字段

- ✅ 操作人
- ✅ 操作时间
- ✅ 操作模块
- ✅ 操作内容
- ✅ 请求参数摘要
- ⚠️ IP / 地理位置（IP 有；地理位置未完整产品化）
- ✅ 结果状态

---

## 5.7 反馈与工单

用于处理用户提交的反馈与问题。

### 功能

- ✅ 反馈列表
- ✅ 反馈详情（列表内详情，非独立路由页）
- ✅ 状态流转（回复 / 关闭 / 重开）
- ✅ 回复用户
- ✅ 标记已处理
- ✅ 导出分析

### 相关后端能力

- `FeedbackController`
- `FeedbackService`
- `FeedbackMapper`

---

## 5.8 运营管理

面向日常平台运营。

### 功能

- ✅ 公告管理
- ⚠️ 版本发布管理（并入系统配置客户端版本，非独立版本发布 CRUD）
- ❌ 推荐位配置
- ✅ Banner 管理
- ⚠️ 首页运营配置（Banner/公告覆盖部分）
- ❌ 活动配置

### 可扩展方向

- ✅ 系统公告下发（发布/下线 + 通知中心）
- ❌ 灰度发布
- ❌ 版本强更配置（`forceUpdate` 字段预留）
- ❌ 运营活动记录

---

## 5.9 风控与安全

用于平台治理和风险控制。

### 功能

- ✅ 登录异常监测（登录日志 + 风险事件）
- ⚠️ IP 限流情况（统计趋势有 rateLimit；独立限流控制台未做）
- ✅ 黑名单管理
- ✅ 敏感词命中记录（审核/统计/风控侧）
- ⚠️ 设备封禁（设备踢下线已落地；长期封禁设备能力有限）
- ✅ 风险会话清理（踢设备 / 处置风险）
- ✅ 可疑行为告警（风险事件）

### 相关后端能力

- `RateLimitService`
- `LoginInterceptor`
- `SensitiveWordService`
- `UserBlacklistMapper`
- `DeviceSessionService`

---

## 5.10 数据统计

用于运营分析与决策支持。

### 建议统计指标

- ✅ 新增用户
- ✅ 活跃用户（登录成功等趋势）
- ✅ 消息发送量
- ⚠️ 群活跃度
- ✅ 文件上传量
- ✅ 反馈处理效率
- ⚠️ 审核处理效率（审核列表/待办有，专项效率指标可增强）
- ✅ 风控拦截趋势

### 图表建议

- ✅ 折线图：趋势
- ✅ 柱状图：模块对比
- ✅ 饼图：类别占比
- ❌ 热力图：活跃时段

---

## 6. 管理端页面结构建议

> 对照现状：✅ 已有独立页/能力 · ⚠️ 合并实现 · ❌ 未做

```text
✅ 登录页
✅ 仪表盘
├── ✅ 数据概览
├── ✅ 实时在线
✅ 用户管理
├── ✅ 用户列表
├── ✅ 用户详情
├── ✅ 设备管理
├── ✅ 黑名单
✅ 内容审核
├── ✅ 朋友圈审核（审核列表按类型）
├── ✅ 评论审核（审核列表按类型）
├── ⚠️ 举报处理（审核/风险事件）
├── ✅ 敏感词命中 / 敏感词管理
✅ 反馈与工单
├── ✅ 反馈列表
├── ✅ 回复记录（详情内）
✅ 权限管理
├── ✅ 用户管理（用户中心）
├── ✅ 角色管理
├── ✅ 权限管理
├── ⚠️ 菜单管理（只读）
✅ 系统配置
├── ✅ 全局/分类配置（注册/登录/密码/邮件/水印等）
├── ✅ 注册配置
├── ✅ 风控配置（登录策略等）
├── ⚠️ 版本管理（客户端配置 Tab）
✅ 日志审计
├── ✅ 操作日志
├── ✅ 登录日志
├── ✅ 设备日志 / 设备管理
├── ✅ 风险事件
✅ 运营管理
├── ✅ 公告管理
├── ✅ Banner 管理
├── ✅ 通知中心
├── ❌ 推荐配置
✅ 统计分析
├── ✅ 用户统计
├── ✅ 内容统计
├── ✅ 风控统计
├── ✅ 反馈统计
```

---

## 7. 接口设计建议

管理端建议统一走 `admin` 前缀或在网关层区分权限域。

### 示例接口分组

- ✅ `/admin/dashboard/*`
- ✅ `/admin/users/*`
- ✅ `/admin/roles/*`
- ✅ `/admin/permissions/*`
- ✅ `/admin/audit-logs/*`
- ✅ `/admin/login-logs/*`
- ✅ `/admin/feedback/*`
- ⚠️ `/admin/compliance/*`（管理端落地为 `/admin/reviews`、`/admin/sensitive-words` 等）
- ⚠️ `/admin/config/*`（落地为 `/admin/settings/*`）
- ⚠️ `/admin/version/*`（并入 `/admin/settings/client`）

### 通用返回格式

建议继续沿用当前后端统一返回结构：

- 成功：`Result.success(...)`
- 失败：`Result.error(...)`
- 统一错误码与消息

### 分页规范

管理端列表类接口建议统一支持：

- `page`
- `size`
- `keyword`
- `status`
- `startTime`
- `endTime`
- `sortBy`
- `sortOrder`

---

## 8. 前端架构建议

### 8.1 目录结构

```text
linkx-admin/
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   ├── router/
│   ├── stores/
│   ├── utils/
│   ├── views/
│   └── App.vue
├── public/
├── index.html
├── package.json
└── vite.config.ts
```

### 8.2 通用布局

建议采用典型后台布局：

- 左侧菜单栏
- 顶部导航栏
- 面包屑
- 内容区
- 右侧抽屉或详情弹窗

### 8.3 设计原则

- 默认桌面浏览器优先
- 页面信息密度高但不拥挤
- 列表页支持搜索、筛选、导出
- 详情页使用抽屉或侧边栏承载
- 危险操作需要二次确认

---

## 9. 开发顺序建议

> 状态说明：**第一至第四阶段已交付**；第五阶段为当前进行项（部分导出/批量/图表已落地，仍需验收与深化）。

### 第一阶段：基础骨架 ✅ 已交付

- 创建 `linkx-admin` 工程
- 接入路由、布局、登录页
- 接入统一请求封装
- 接入权限守卫
- 接入基础组件库

### 第二阶段：基础后台能力 ✅ 已交付

- 仪表盘
- 用户管理
- 角色与权限管理
- 审计日志
- 登录日志

### 第三阶段：业务治理能力 ✅ 已交付

- 反馈管理
- 内容审核
- 敏感词管理
- 黑名单管理
- 设备管理

### 第四阶段：运营能力 ✅ 已交付

- 公告管理
- 版本管理（并入系统配置）
- 配置中心
- 统计分析

### 第五阶段：完善体验 🔄 进行中

- 导出能力（关键列表已具备，继续补齐与验收）
- 批量操作（审核、风险事件等已具备）
- 高级筛选
- 图表看板（统计页已具备，可继续增强）
- 操作留痕与生产联调验收

---

## 10. 数据模型建议

如果后端还未完全覆盖管理端所需实体，建议补充以下表：

- `sys_menu`
- `sys_permission`
- `sys_role_menu`
- `sys_operation_log`
- `sys_login_log`
- `sys_admin_notice`
- `sys_banner`
- `sys_feedback_reply`
- `sys_review_task`
- `sys_risk_event`

---

## 11. 安全要求

管理端属于高权限入口，必须重点保护。

### 必须项

- ✅ 独立登录入口
- ✅ 强制 HTTPS（`ProductionSecurityValidator` 生产强制 `REQUIRE_HTTPS=true`）
- ✅ 管理员强密码策略（密码策略可配置）
- ✅ 登录失败锁定
- ✅ IP 风险提示（登录审计有 IP；管理端登录返回 `newLoginIp` 并前端告警）
- ✅ 关键操作二次确认
- ✅ 操作审计不可关闭
- ✅ 生产环境禁止明文/弱密钥（JWT/DB/Redis/MinIO 启动硬校验）

### 建议项

- ✅ 双因素认证（管理端 TOTP）
- ⚠️ 登录设备绑定（设备会话可管，强绑定策略未完整）
- ❌ 高危操作短信或邮箱验证
- ✅ 敏感接口权限白名单（`@RequirePermission`）

---

## 12. 与客户端的边界

客户端与管理端建议保持清晰边界：

- 客户端面向普通用户使用
- 管理端面向运营、审核、安全人员使用
- 两者可共享后端服务与部分 DTO/VO 设计思想
- 不建议复用客户端 UI 组件作为后台主框架

---

## 13. 推荐里程碑

> **M1–M4 已交付。** 不要再从 M1 重新开工；后续任务从 M5 推进。

### M1 ✅ 已交付

- 管理端工程初始化
- 登录鉴权
- 基础布局
- 菜单权限

### M2 ✅ 已交付

- 用户管理
- 角色权限
- 审计日志

### M3 ✅ 已交付

- 反馈管理
- 内容审核
- 黑名单与设备管理

### M4 ✅ 已交付

- 系统配置
- 公告与版本（版本配置并入系统配置）
- 数据统计

### M5 🔄 进行中（当前重点）

- 全链路联调
- 生产权限校验
- 风控与审计完善

---

## 14. 结论

当前 LinkX 项目已进入“客户端成熟、后端完善、**管理端 M1–M4 已交付**”的阶段。独立工程 `linkx-admin` 与后端 `/admin/**` 已形成第一版闭环（登录、RBAC、用户、审核、日志、运营配置、统计等）。

下一步不要重建工程或重复实现已有模块，应聚焦：

- M5：按角色联调验收、生产权限与安全加固
- 运营增强：版本强更/灰度、菜单排序等产品化能力
- 文档维护：以本文档后半设计清单对照缺口，而不是从零排期

如果需要，后续可以继续把本文档拆成：

- 管理端页面清单
- 管理端接口清单
- 管理端数据库设计
- 管理端缺口与 M5 验收清单

---

## 15. 更落地的管理端建设清单

下面给出一版可以直接进入实施阶段的落地清单，便于你后续拆任务、排期和分工。

### 15.1 页面清单

建议先做以下页面：

- ✅ 登录页
- ✅ 仪表盘
- ✅ 用户列表
- ✅ 用户详情
- ✅ 角色列表
- ✅ 权限列表
- ⚠️ 菜单管理（只读树；无前端增删改/排序）
- ✅ 操作日志
- ✅ 登录日志
- ✅ 反馈列表
- ✅ 反馈详情（页内详情，非 `/feedback/:id` 独立路由）
- ✅ 违规内容列表
- ✅ 敏感词管理
- ✅ 黑名单管理
- ✅ 设备管理
- ⚠️ 版本管理（并入系统配置客户端 Tab；独立页已下线）
- ✅ 公告管理
- ✅ 系统配置

另已落地（超出首批清单）：风险事件、通知中心、Banner、统计分析、个人中心/TOTP。

### 15.2 菜单原型

建议后台菜单按以下层级组织（对照 `sys_admin_menu`）：

```text
✅ 控制台
├── ✅ 仪表盘
✅ 用户中心
├── ✅ 用户列表
├── ✅ 用户详情（隐藏路由）
├── ✅ 黑名单管理
├── ✅ 设备管理
✅ 权限中心
├── ✅ 角色管理
├── ✅ 权限管理
├── ⚠️ 菜单管理（只读）
✅ 日志中心
├── ✅ 操作日志
├── ✅ 登录日志
├── ✅ 风险事件
✅ 审核中心
├── ✅ 反馈管理
├── ✅ 违规内容
├── ✅ 敏感词管理
✅ 运营中心
├── ✅ 公告管理
├── ✅ 通知中心
├── ✅ Banner 管理
├── ⚠️ 版本管理（已隐藏，并入系统配置）
├── ✅ 系统配置
├── ✅ 统计分析
```

### 15.3 接口清单（优先级）

建议先补齐以下接口：

#### 认证与权限
- ✅ `POST /admin/auth/login`
- ✅ `POST /admin/auth/logout`
- ✅ `GET /admin/auth/me`
- ✅ `GET /admin/auth/menus`
- ✅ `GET /admin/auth/permissions`

#### 用户管理
- ✅ `GET /admin/users`
- ✅ `GET /admin/users/{id}`
- ✅ `PUT /admin/users/{id}`
- ✅ `POST /admin/users/{id}/freeze`
- ✅ `POST /admin/users/{id}/unfreeze`
- ✅ `POST /admin/users/{id}/ban`
- ✅ `POST /admin/users/{id}/unban`
- ✅ `GET /admin/users/{id}/devices`
- ✅ `GET /admin/users/{id}/logins`

#### 权限管理
- ✅ `GET /admin/roles`
- ✅ `POST /admin/roles`
- ✅ `PUT /admin/roles/{id}`
- ✅ `DELETE /admin/roles/{id}`
- ✅ `GET /admin/permissions`
- ❌ `POST /admin/roles/{id}/permissions`（未实现；角色菜单授权用 `PUT /admin/roles/{id}/menus`）
- ⚠️ `POST /admin/roles/{id}/users`（已实现为 `PUT /admin/roles/{id}/users`）

#### 审计与日志
- ✅ `GET /admin/audit-logs`
- ✅ `GET /admin/login-logs`
- ✅ `GET /admin/risk-events`

#### 反馈与审核
- ✅ `GET /admin/feedback`
- ✅ `GET /admin/feedback/{id}`
- ✅ `POST /admin/feedback/{id}/reply`
- ✅ `POST /admin/feedback/{id}/close`
- ✅ `GET /admin/reviews`
- ✅ `POST /admin/reviews/{id}/approve`
- ✅ `POST /admin/reviews/{id}/reject`

#### 运营配置
- ✅ `GET /admin/notices`
- ✅ `POST /admin/notices`
- ✅ `PUT /admin/notices/{id}`
- ✅ `DELETE /admin/notices/{id}`
- ⚠️ `GET /admin/versions`（无独立版本 API；版本字段走 `/admin/settings` 客户端配置）
- ❌ `POST /admin/versions`
- ❌ `PUT /admin/versions/{id}`
- ✅ `GET /admin/settings`
- ⚠️ `PUT /admin/settings`（拆分为 `/admin/settings/{register|login|password|client|mail|...}`）

### 15.4 数据表建议

如果需要补数据库表，建议优先做这些：

- ✅ `sys_admin_user`：复用普通用户表 `sys_user` + 管理角色（不单独建管理员表）
- ✅ `sys_menu`：落地为 `sys_admin_menu`
- ✅ `sys_permission`：复用现有 `sys_permission`
- ✅ `sys_role_menu`：落地为 `sys_admin_role_menu`
- ✅ `sys_role_permission`：复用现有 `sys_role_permission`
- ✅ `sys_operation_log`：复用 `sys_audit_log`
- ✅ `sys_login_log`：复用 `sys_login_audit`
- ✅ `sys_notice`：落地为 `sys_admin_notice`
- ✅ `sys_banner`：`sys_banner`
- ✅ `sys_review_task`：`sys_review_task`
- ✅ `sys_risk_event`：`sys_risk_event`
- ⚠️ `sys_feedback_reply`：反馈回复落在 `sys_feedback.reply` 字段（非独立回复表）

### 15.5 前端页面开发顺序

> 下列顺序对应 M1–M4，**现已交付**；仅作历史参考与能力对照。

1. 登录页和鉴权 ✅
2. 后台布局和菜单 ✅
3. 仪表盘 ✅
4. 用户列表与详情 ✅
5. 角色权限 ✅
6. 操作日志与登录日志 ✅
7. 反馈管理 ✅
8. 审核管理 ✅
9. 系统配置与版本管理 ✅
10. 统计分析与图表 ✅

### 15.6 第一版验收标准

第一版管理端应达到以下标准（**已作为交付基线，M5 需持续回归**）：

- ✅ 能登录、退出、刷新会话
- ✅ 菜单随角色动态展示
- ✅ 可查看用户和用户详情
- ✅ 可查看日志与反馈
- ✅ 可处理基础审核任务
- ✅ 所有危险操作有确认弹窗
- ✅ 所有关键动作进入审计日志

### 15.7 推荐的项目命名

独立工程已命名为 **`linkx-admin`**（曾备选 `linkx-console` / `linkx-backoffice`）。后续维护沿用该名称即可。

---

## 16. `linkx-admin` 初始化方案

> **工程已初始化并交付。** 本节保留为技术栈与目录约定说明，勿再按步骤从零创建项目。

### 16.1 技术栈

- Vue 3
- TypeScript
- Vite
- Pinia
- Vue Router
- Naive UI
- Axios
- ECharts
- dayjs

### 16.2 初始化步骤

1. 创建 `linkx-admin` 工程
2. 配置 TypeScript、ESLint、Prettier、Vitest
3. 配置基础环境变量
4. 接入请求封装和统一响应处理
5. 接入登录页与 Token 持久化
6. 接入后台布局、侧边栏、顶部栏
7. 接入权限路由守卫
8. 接入基础表格、抽屉、弹窗、表单组件
9. 完成仪表盘与用户管理首批页面

### 16.3 建议目录结构

```text
linkx-admin/
├── public/
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   ├── router/
│   ├── stores/
│   ├── styles/
│   ├── types/
│   ├── utils/
│   ├── views/
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── .env.example
```

### 16.4 初始化页面

首批只保留必要页面：

- ✅ `LoginView.vue`
- ✅ `AdminLayout.vue`
- ✅ `DashboardView.vue`
- ✅ `UserListView.vue`
- ✅ `RoleListView.vue`
- ✅ `PermissionListView.vue`
- ✅ `AuditLogView.vue`
- ✅ `LoginLogView.vue`
- ✅ `FeedbackListView.vue`
- ✅ `SettingView.vue`

### 16.5 通用能力

建议第一版就统一好这些能力：

- ✅ 请求拦截器自动带 Token
- ✅ 响应统一错误提示
- ✅ 401 自动退出登录（含 refresh 失败）
- ✅ 权限不足时跳转无权限页
- ✅ 列表分页查询封装
- ✅ 表单校验统一化
- ✅ 文件导出统一下载处理

---

## 17. 后台路由和菜单配置

下面给出一份适合第一版后台的路由与菜单设计草案。

### 17.1 路由结构

```text
/
├── ✅ /login
├── ✅ /forbidden
├── ✅ /404
└── ✅ /admin
    ├── ✅ /dashboard
    ├── ✅ /users
    ├── ✅ /users/:id
    ├── ✅ /blacklist
    ├── ✅ /devices
    ├── ✅ /roles
    ├── ✅ /permissions
    ├── ✅ /menus
    ├── ✅ /audit-logs
    ├── ✅ /login-logs
    ├── ✅ /feedback
    ├── ⚠️ /feedback/:id（详情在列表页内，无独立路由）
    ├── ✅ /reviews
    ├── ✅ /sensitive-words
    ├── ✅ /risk-events
    ├── ✅ /settings
    ├── ✅ /notices
    ├── ✅ /notice-inbox
    ├── ✅ /banners
    ├── ⚠️ /versions（redirect → /settings?tab=client）
    ├── ✅ /statistics
    └── ✅ /profile
```

### 17.2 前端路由示例

```ts
export const adminRoutes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/LoginView.vue'),
    meta: { public: true },
  },
  {
    path: '/admin',
    component: () => import('@/layouts/AdminLayout.vue'),
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/DashboardView.vue'),
        meta: { title: '仪表盘', icon: 'Dashboard', permission: 'admin:dashboard:view' },
      },
      {
        path: 'users',
        name: 'UserList',
        component: () => import('@/views/UserListView.vue'),
        meta: { title: '用户管理', icon: 'Users', permission: 'admin:user:list' },
      },
      {
        path: 'roles',
        name: 'RoleList',
        component: () => import('@/views/RoleListView.vue'),
        meta: { title: '角色管理', icon: 'Shield', permission: 'admin:role:list' },
      },
      {
        path: 'permissions',
        name: 'PermissionList',
        component: () => import('@/views/PermissionListView.vue'),
        meta: { title: '权限管理', icon: 'Key', permission: 'admin:permission:list' },
      },
      {
        path: 'menus',
        name: 'MenuList',
        component: () => import('@/views/MenuListView.vue'),
        meta: { title: '菜单管理', icon: 'Menu', permission: 'admin:menu:list' },
      },
      {
        path: 'audit-logs',
        name: 'AuditLogs',
        component: () => import('@/views/AuditLogView.vue'),
        meta: { title: '操作日志', icon: 'History', permission: 'admin:audit:list' },
      },
      {
        path: 'login-logs',
        name: 'LoginLogs',
        component: () => import('@/views/LoginLogView.vue'),
        meta: { title: '登录日志', icon: 'LogIn', permission: 'admin:login-log:list' },
      },
      {
        path: 'feedback',
        name: 'FeedbackList',
        component: () => import('@/views/FeedbackListView.vue'),
        meta: { title: '反馈管理', icon: 'Message', permission: 'admin:feedback:list' },
      },
      {
        path: 'reviews',
        name: 'ReviewList',
        component: () => import('@/views/ReviewListView.vue'),
        meta: { title: '内容审核', icon: 'Check', permission: 'admin:review:list' },
      },
      {
        path: 'risk-events',
        name: 'RiskEvents',
        component: () => import('@/views/RiskEventView.vue'),
        meta: { title: '风险事件', icon: 'AlertTriangle', permission: 'admin:risk-event:list' },
      },
      {
        path: 'settings',
        name: 'Settings',
        component: () => import('@/views/SettingView.vue'),
        meta: { title: '系统配置', icon: 'Settings', permission: 'admin:setting:view' },
      },
      {
        path: 'notices',
        name: 'Notices',
        component: () => import('@/views/NoticeView.vue'),
        meta: { title: '公告管理', icon: 'Bell', permission: 'admin:notice:list' },
      },
      {
        path: 'versions',
        name: 'Versions',
        component: () => import('@/views/VersionView.vue'),
        meta: { title: '版本管理', icon: 'Package', permission: 'admin:version:list' },
      },
      {
        path: 'statistics',
        name: 'Statistics',
        component: () => import('@/views/StatisticsView.vue'),
        meta: { title: '统计分析', icon: 'Chart', permission: 'admin:statistics:view' },
      },
    ],
  },
]
```

### 17.3 菜单配置建议

建议菜单数据与路由同源，后端返回一棵树，前端动态渲染。

```ts
export interface AdminMenuItem {
  id: string
  parentId?: string
  name: string
  title: string
  path: string
  icon?: string
  type: 'dir' | 'menu' | 'button'
  permission?: string
  sort: number
  visible: boolean
  children?: AdminMenuItem[]
}
```

### 17.4 菜单示例数据

```json
[
  {
    "id": "dashboard",
    "title": "仪表盘",
    "path": "/admin/dashboard",
    "type": "menu",
    "permission": "admin:dashboard:view",
    "sort": 1,
    "visible": true
  },
  {
    "id": "user",
    "title": "用户管理",
    "path": "/admin/users",
    "type": "menu",
    "permission": "admin:user:list",
    "sort": 2,
    "visible": true
  },
  {
    "id": "rbac",
    "title": "权限中心",
    "path": "/admin/rbac",
    "type": "dir",
    "sort": 3,
    "visible": true,
    "children": [
      {
        "id": "role",
        "title": "角色管理",
        "path": "/admin/roles",
        "type": "menu",
        "permission": "admin:role:list",
        "sort": 1,
        "visible": true
      },
      {
        "id": "permission",
        "title": "权限管理",
        "path": "/admin/permissions",
        "type": "menu",
        "permission": "admin:permission:list",
        "sort": 2,
        "visible": true
      },
      {
        "id": "menu",
        "title": "菜单管理",
        "path": "/admin/menus",
        "type": "menu",
        "permission": "admin:menu:list",
        "sort": 3,
        "visible": true
      }
    ]
  }
]
```

### 17.5 菜单生成原则

- 先按权限过滤，再按路由渲染
- 菜单与页面权限绑定
- 按钮权限单独处理，不进入侧边栏
- 菜单排序由后端控制
- 超级管理员可见全部菜单

---

## 18. 后端 `/admin/**` 接口设计草案

下面给出一版适合 Spring Boot 实现的后台接口设计草案。

### 18.1 统一约定

- 所有后台接口以 `/admin` 开头
- 登录后返回后台专用 Token 或复用统一 Token 但带 `role=ADMIN`
- 所有接口都需要鉴权
- 关键接口需要权限码
- 列表接口统一分页
- 所有写操作进入审计日志

### 18.2 鉴权接口

#### 登录

- `POST /admin/auth/login`

请求示例：

```json
{
  "username": "admin",
  "password": "******",
  "captchaId": "xxx",
  "captchaCode": "1234"
}
```

响应示例：

```json
{
  "accessToken": "...",
  "refreshToken": "...",
  "expiresIn": 7200,
  "user": {
    "id": 1,
    "username": "admin",
    "nickname": "超级管理员",
    "roles": ["SUPER_ADMIN"],
    "permissions": ["admin:*"]
  }
}
```

#### 当前登录信息

- `GET /admin/auth/me`
- `GET /admin/auth/menus`
- `GET /admin/auth/permissions`
- `POST /admin/auth/logout`
- `POST /admin/auth/refresh`

### 18.3 仪表盘接口

- `GET /admin/dashboard/summary`
- `GET /admin/dashboard/trends`
- `GET /admin/dashboard/realtime`
- `GET /admin/dashboard/pending-tasks`

### 18.4 用户管理接口

- `GET /admin/users`
- `GET /admin/users/{id}`
- `PUT /admin/users/{id}`
- `POST /admin/users/{id}/freeze`
- `POST /admin/users/{id}/unfreeze`
- `POST /admin/users/{id}/ban`
- `POST /admin/users/{id}/unban`
- `GET /admin/users/{id}/devices`
- `GET /admin/users/{id}/logins`
- `GET /admin/users/export`

### 18.5 角色与权限接口

- `GET /admin/roles`
- `POST /admin/roles`
- `PUT /admin/roles/{id}`
- `DELETE /admin/roles/{id}`
- `GET /admin/roles/{id}`
- `GET /admin/roles/{id}/permissions`
- `PUT /admin/roles/{id}/permissions`
- `GET /admin/permissions`
- `POST /admin/permissions`
- `PUT /admin/permissions/{id}`
- `DELETE /admin/permissions/{id}`
- `GET /admin/menus`
- `POST /admin/menus`
- `PUT /admin/menus/{id}`
- `DELETE /admin/menus/{id}`

### 18.6 日志接口

- `GET /admin/audit-logs`
- `GET /admin/audit-logs/{id}`
- `GET /admin/login-logs`
- `GET /admin/login-logs/{id}`
- `GET /admin/risk-events`
- `POST /admin/risk-events/{id}/handle`

### 18.7 反馈与审核接口

- `GET /admin/feedback`
- `GET /admin/feedback/{id}`
- `POST /admin/feedback/{id}/reply`
- `POST /admin/feedback/{id}/close`
- `GET /admin/reviews`
- `GET /admin/reviews/{id}`
- `POST /admin/reviews/{id}/approve`
- `POST /admin/reviews/{id}/reject`
- `POST /admin/reviews/batch`

### 18.8 运营与配置接口

- `GET /admin/notices`
- `POST /admin/notices`
- `PUT /admin/notices/{id}`
- `DELETE /admin/notices/{id}`
- `GET /admin/versions`
- `POST /admin/versions`
- `PUT /admin/versions/{id}`
- `DELETE /admin/versions/{id}`
- `GET /admin/settings`
- `PUT /admin/settings`
- `GET /admin/statistics/overview`
- `GET /admin/statistics/users`
- `GET /admin/statistics/content`
- `GET /admin/statistics/risk`

### 18.9 推荐返回结构

列表接口建议统一结构：

```json
{
  "items": [],
  "page": 1,
  "size": 20,
  "total": 0
}
```

详情接口建议统一结构：

```json
{
  "id": 1,
  "name": "示例"
}
```

### 18.10 推荐权限码规范

建议统一前缀：

- `admin:dashboard:view`
- `admin:user:list`
- `admin:user:edit`
- `admin:user:ban`
- `admin:role:list`
- `admin:role:edit`
- `admin:permission:list`
- `admin:menu:edit`
- `admin:audit:list`
- `admin:feedback:reply`
- `admin:review:approve`
- `admin:setting:edit`

### 18.11 建议后端控制器拆分

```text
com.linkx.server.controller.admin
├── AdminAuthController
├── AdminDashboardController
├── AdminUserController
├── AdminRoleController
├── AdminPermissionController
├── AdminMenuController
├── AdminAuditLogController
├── AdminLoginLogController
├── AdminFeedbackController
├── AdminReviewController
├── AdminRiskEventController
├── AdminNoticeController
├── AdminVersionController
└── AdminSettingController
```

### 18.12 建议服务层拆分

```text
com.linkx.server.service.admin
├── AdminAuthService
├── AdminDashboardService
├── AdminUserService
├── AdminRoleService
├── AdminPermissionService
├── AdminMenuService
├── AdminAuditLogService
├── AdminLoginLogService
├── AdminFeedbackService
├── AdminReviewService
├── AdminRiskEventService
├── AdminNoticeService
├── AdminVersionService
└── AdminSettingService
```

---

## 19. 推荐实现节奏

> 以下四周节奏对应 **M1–M4，现已交付**，仅作历史排期参考。当前不要按此从第 1 周重做。

### 第 1 周 ✅

- 建 `linkx-admin`
- 搭登录页和布局
- 打通 `/admin/auth/login`、`/admin/auth/me`、`/admin/auth/menus`

### 第 2 周 ✅

- 用户列表与详情
- 角色与权限页面
- 菜单管理页面

### 第 3 周 ✅

- 操作日志、登录日志
- 反馈管理
- 审核列表

### 第 4 周 ✅

- 系统配置
- 版本管理（并入系统配置）
- 公告管理
- 统计看板

### 当前节奏（M5）

- 按角色冒烟：超管 / 运营 / 审核 / 安全 / 只读
- 核对菜单、按钮权限码与接口鉴权一致性
- 补齐导出/批量/筛选缺口与生产安全验收
- 评估强更、灰度、菜单排序等增强项

---

## 20. 管理端菜单数据的数据库表设计

下面给出一版可直接用于实现动态后台菜单的表结构设计。该设计同时支持菜单、按钮权限、页面权限和接口权限的统一管理。

### 20.1 设计目标

- 支持树形菜单
- 支持按钮级权限
- 支持接口级权限码
- 支持菜单排序、隐藏、外链、缓存、图标
- 支持角色绑定菜单与权限点
- 支持前后端动态路由生成

### 20.2 `sys_admin_menu` 表

用于存储后台菜单节点。

```sql
CREATE TABLE sys_admin_menu (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  parent_id BIGINT NOT NULL DEFAULT 0 COMMENT '父菜单ID，0表示根节点',
  name VARCHAR(64) NOT NULL COMMENT '菜单名称标识',
  title VARCHAR(64) NOT NULL COMMENT '菜单显示名称',
  path VARCHAR(255) NOT NULL COMMENT '路由路径',
  component VARCHAR(255) DEFAULT NULL COMMENT '前端组件路径',
  redirect VARCHAR(255) DEFAULT NULL COMMENT '重定向路径',
  icon VARCHAR(64) DEFAULT NULL COMMENT '图标名称',
  menu_type VARCHAR(16) NOT NULL COMMENT 'dir/menu/button/api',
  permission_code VARCHAR(128) DEFAULT NULL COMMENT '权限码',
  sort_order INT NOT NULL DEFAULT 0 COMMENT '排序值',
  hidden TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否隐藏',
  cacheable TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否缓存',
  external_link TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否外链',
  keep_alive TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否保持路由缓存',
  status TINYINT(1) NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  remark VARCHAR(255) DEFAULT NULL COMMENT '备注',
  created_by BIGINT DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_by BIGINT DEFAULT NULL,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted TINYINT(1) NOT NULL DEFAULT 0,
  KEY idx_parent_id (parent_id),
  KEY idx_permission_code (permission_code),
  KEY idx_sort_order (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='后台菜单表';
```

### 20.3 字段说明

- `parent_id`：菜单树父节点，顶层为 `0`
- `menu_type`：区分目录、菜单、按钮、接口权限
- `permission_code`：用于前端按钮权限与后端接口鉴权
- `component`：前端懒加载组件路径
- `hidden`：是否在侧边栏隐藏
- `cacheable`：是否加入缓存策略
- `external_link`：是否跳转外部地址
- `keep_alive`：是否维持页面状态

### 20.4 `sys_admin_role_menu` 表

用于角色绑定菜单和权限点。

```sql
CREATE TABLE sys_admin_role_menu (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_id BIGINT NOT NULL COMMENT '角色ID',
  menu_id BIGINT NOT NULL COMMENT '菜单ID',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_role_menu (role_id, menu_id),
  KEY idx_role_id (role_id),
  KEY idx_menu_id (menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

### 20.5 `sys_admin_permission` 表

如果需要将按钮权限、接口权限独立出来，可以拆一张权限点表。

```sql
CREATE TABLE sys_admin_permission (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  perm_key VARCHAR(128) NOT NULL COMMENT '权限码，例如 admin:user:edit',
  perm_name VARCHAR(128) NOT NULL COMMENT '权限名称',
  perm_type VARCHAR(16) NOT NULL COMMENT 'button/api/page',
  menu_id BIGINT DEFAULT NULL COMMENT '所属菜单ID',
  status TINYINT(1) NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  remark VARCHAR(255) DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted TINYINT(1) NOT NULL DEFAULT 0,
  UNIQUE KEY uk_perm_key (perm_key),
  KEY idx_menu_id (menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='后台权限点表';
```

### 20.6 `sys_admin_role_permission` 表

用于角色绑定权限点。

```sql
CREATE TABLE sys_admin_role_permission (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_id BIGINT NOT NULL COMMENT '角色ID',
  permission_id BIGINT NOT NULL COMMENT '权限ID',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_role_permission (role_id, permission_id),
  KEY idx_role_id (role_id),
  KEY idx_permission_id (permission_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';
```

### 20.7 菜单示例初始化数据

```sql
INSERT INTO sys_admin_menu (parent_id, name, title, path, component, icon, menu_type, permission_code, sort_order, hidden, cacheable, external_link, keep_alive, status) VALUES
(0, 'dashboard', '仪表盘', '/admin/dashboard', 'layouts/AdminLayout', 'Dashboard', 'menu', 'admin:dashboard:view', 1, 0, 1, 0, 1, 1),
(0, 'user', '用户管理', '/admin/users', 'views/UserListView', 'Users', 'menu', 'admin:user:list', 2, 0, 1, 0, 1, 1),
(0, 'rbac', '权限中心', '/admin/rbac', NULL, 'Shield', 'dir', NULL, 3, 0, 1, 0, 1, 1),
(3, 'role', '角色管理', '/admin/roles', 'views/RoleListView', 'Badge', 'menu', 'admin:role:list', 1, 0, 1, 0, 1, 1),
(3, 'permission', '权限管理', '/admin/permissions', 'views/PermissionListView', 'Key', 'menu', 'admin:permission:list', 2, 0, 1, 0, 1, 1),
(3, 'menu', '菜单管理', '/admin/menus', 'views/MenuListView', 'Menu', 'menu', 'admin:menu:list', 3, 0, 1, 0, 1, 1);
```

### 20.8 推荐索引与约束

- `parent_id`：支持树查询
- `permission_code`：支持权限定位
- `role_id + menu_id` 唯一约束：避免重复授权
- `perm_key` 唯一约束：避免权限码冲突

### 20.9 后端实现建议

建议新增：

- `AdminMenuEntity`
- `AdminMenuMapper`
- `AdminMenuService`
- `AdminMenuController`
- `AdminMenuVO`
- `AdminMenuDTO`

并支持以下能力：

- 查询树形菜单
- 新增菜单
- 编辑菜单
- 删除菜单
- 调整排序
- 角色授权菜单
- 生成前端路由树

---

## 21. 完整的前后端联调接口清单

下面给出一版完整的联调接口清单，方便前后端按模块同步开发。

### 21.1 认证模块

- ✅ `POST /admin/auth/login`
- ✅ `POST /admin/auth/logout`
- ✅ `POST /admin/auth/refresh`
- ✅ `GET /admin/auth/me`
- ✅ `GET /admin/auth/menus`
- ✅ `GET /admin/auth/permissions`
- ✅ 另已落地：`/admin/auth/config`、TOTP 系列、`PUT /admin/auth/profile`

#### 联调要点

- ✅ 登录成功返回 Token、用户信息、权限列表
- ✅ `/me` 接口返回当前管理员基本资料
- ✅ `/menus` 接口返回树形菜单
- ✅ `/permissions` 接口返回扁平权限码集合
- ✅ 401 统一跳转登录页

### 21.2 仪表盘模块

- ✅ `GET /admin/dashboard/summary`
- ✅ `GET /admin/dashboard/trends`
- ✅ `GET /admin/dashboard/realtime`
- ✅ `GET /admin/dashboard/pending-tasks`
- ✅ 另已落地：`GET /admin/events/stream`（SSE）

#### 联调要点

- ✅ `summary` 返回核心指标卡片
- ✅ `trends` 返回折线图所需时间序列
- ✅ `realtime` 返回在线状态和实时事件流
- ✅ `pending-tasks` 返回待处理任务列表

### 21.3 用户管理模块

- ✅ `GET /admin/users`
- ✅ `GET /admin/users/{id}`
- ✅ `PUT /admin/users/{id}`
- ✅ `POST /admin/users/{id}/freeze`
- ✅ `POST /admin/users/{id}/unfreeze`
- ✅ `POST /admin/users/{id}/ban`
- ✅ `POST /admin/users/{id}/unban`
- ✅ `GET /admin/users/{id}/devices`
- ✅ `GET /admin/users/{id}/logins`
- ✅ `GET /admin/users/export`
- ✅ 另已落地：`POST /admin/users/{id}/reset-password`

#### 联调要点

- ✅ 列表支持分页、排序、关键词、状态筛选
- ✅ 详情页需要展示基础信息、设备、登录记录、风险标签
- ✅ 冻结/封禁操作需要二次确认
- ⚠️ 导出建议异步生成文件（当前同步 CSV 下载）

### 21.4 角色与权限模块

- ✅ `GET /admin/roles`
- ✅ `POST /admin/roles`
- ✅ `PUT /admin/roles/{id}`
- ✅ `DELETE /admin/roles/{id}`
- ✅ `GET /admin/roles/{id}`
- ✅ `GET /admin/roles/{id}/menus`
- ✅ `PUT /admin/roles/{id}/menus`
- ❌ `GET /admin/roles/{id}/permissions`
- ❌ `PUT /admin/roles/{id}/permissions`
- ✅ `GET /admin/roles/{id}/users`
- ✅ `PUT /admin/roles/{id}/users`
- ✅ `GET /admin/permissions`
- ✅ `POST /admin/permissions`
- ✅ `PUT /admin/permissions/{id}`
- ✅ `DELETE /admin/permissions/{id}`
- ✅ `GET /admin/menus`
- ⚠️ `POST /admin/menus`（后端有；前端未接）
- ⚠️ `PUT /admin/menus/{id}`（后端有；前端未接）
- ⚠️ `DELETE /admin/menus/{id}`（后端有；前端未接）
- ❌ `POST /admin/menus/reorder`

#### 联调要点

- ✅ 菜单树与权限树前端可直接渲染
- ✅ 角色授权后需要立即刷新权限缓存
- ❌ 菜单排序建议支持拖拽保存

### 21.5 日志模块

- ✅ `GET /admin/audit-logs`
- ✅ `GET /admin/audit-logs/{id}`
- ✅ `GET /admin/login-logs`
- ✅ `GET /admin/login-logs/{id}`
- ✅ `GET /admin/risk-events`
- ✅ `GET /admin/risk-events/{id}`
- ✅ `POST /admin/risk-events/{id}/handle`
- ✅ `GET /admin/risk-events/export`
- ✅ 另已落地：审计/登录日志 export、风险事件 batch

#### 联调要点

- ✅ 支持按时间、操作人、模块、结果状态过滤
- ✅ 详情页展示请求参数摘要和处理结果
- ✅ 高危事件需要支持处置状态流转

### 21.6 反馈模块

- ✅ `GET /admin/feedback`
- ✅ `GET /admin/feedback/{id}`
- ✅ `POST /admin/feedback/{id}/reply`
- ✅ `POST /admin/feedback/{id}/close`
- ✅ `POST /admin/feedback/{id}/reopen`
- ✅ `GET /admin/feedback/export`

#### 联调要点

- ✅ 反馈详情应包含用户信息、附件、处理记录
- ✅ 回复后前端应刷新状态
- ✅ 已关闭反馈支持重新打开

### 21.7 审核模块

- ✅ `GET /admin/reviews`
- ✅ `GET /admin/reviews/{id}`
- ✅ `POST /admin/reviews/{id}/approve`
- ✅ `POST /admin/reviews/{id}/reject`
- ✅ `POST /admin/reviews/batch`
- ✅ `GET /admin/reviews/export`

#### 联调要点

- ✅ 审核列表按内容类型、风险等级、状态筛选
- ✅ 审核动作必须记录审核人、意见和时间
- ✅ 批量审核需明确失败项返回

### 21.8 运营模块

- ✅ `GET /admin/notices`
- ✅ `POST /admin/notices`
- ✅ `PUT /admin/notices/{id}`
- ✅ `DELETE /admin/notices/{id}`
- ✅ `POST /admin/notices/{id}/publish`
- ✅ `POST /admin/notices/{id}/unpublish`
- ✅ 另已落地：Banner CRUD/发布、`GET /admin/notices/inbox`
- ⚠️ `GET /admin/versions`（无独立 Controller；版本走 settings/client）
- ❌ `POST /admin/versions`
- ❌ `PUT /admin/versions/{id}`
- ❌ `DELETE /admin/versions/{id}`
- ❌ `POST /admin/versions/{id}/publish`

#### 联调要点

- ✅ 公告需要发布状态（版本为配置字段，非发布流）
- ✅ 发布动作应记审计日志
- ❌ 版本管理需支持强更标记和生效范围（`forceUpdate` 预留）

### 21.9 系统配置模块

- ✅ `GET /admin/settings`
- ⚠️ `PUT /admin/settings`（拆分为 register/login/password/client/mail/mail-templates/admin 等）
- ❌ `GET /admin/settings/public`
- ❌ `GET /admin/settings/security`（登录/密码等分类 PUT 已覆盖安全相关）
- ❌ `GET /admin/settings/upload`
- ❌ `GET /admin/settings/notification`

#### 联调要点

- ✅ 配置可按分类返回/保存
- ✅ 支持保存后立即生效或延迟生效
- ✅ 敏感配置需限制可见权限

### 21.10 统计模块

- ✅ `GET /admin/statistics/overview`
- ✅ `GET /admin/statistics/users`
- ✅ `GET /admin/statistics/content`
- ✅ `GET /admin/statistics/risk`
- ✅ `GET /admin/statistics/feedback`
- ✅ `GET /admin/statistics/export`

#### 联调要点

- ✅ 图表数据结构统一
- ✅ 前端需兼容空数据和异常缺省值
- ✅ 导出建议和列表筛选条件保持一致

### 21.11 通用联调规范

#### 请求规范

- 统一携带 `Authorization`
- 列表请求统一 `page`、`size`
- 时间范围统一 `startTime`、`endTime`
- 关键操作统一幂等键或防重复提交

#### 响应规范

- 成功返回 `Result.success`
- 失败返回 `Result.error`
- 列表统一 `{ items, page, size, total }`
- 详情统一对象结构，不包多余层级

#### 错误码建议

- `401`：未登录或 Token 失效
- `403`：权限不足
- `400`：参数错误
- `409`：资源冲突
- `500`：服务器错误

#### 前后端联调顺序

1. 认证接口
2. 菜单接口
3. 用户列表接口
4. 角色权限接口
5. 日志接口
6. 反馈和审核接口
7. 运营与配置接口
8. 统计接口

### 21.12 建议交付物

联调时建议同步产出以下内容：

- 接口 Swagger 文档
- Postman / Apifox 集合
- 菜单 JSON 样例
- 权限码字典
- 前端页面原型
- 数据表初始化 SQL

---

## 22. 后台菜单表的 MySQL 版本详细建表 SQL

下面给出适合直接落库使用的 MySQL 版本详细建表 SQL。该版本补充了更完整的字符集、索引、约束和审计字段，便于后续做后台菜单、权限和路由的动态管理。

### 22.1 后台菜单表

```sql
CREATE TABLE `sys_admin_menu` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `parent_id` BIGINT NOT NULL DEFAULT 0 COMMENT '父菜单ID，根节点为0',
  `name` VARCHAR(64) NOT NULL COMMENT '菜单唯一标识',
  `title` VARCHAR(64) NOT NULL COMMENT '菜单展示名称',
  `path` VARCHAR(255) NOT NULL COMMENT '前端路由路径',
  `component` VARCHAR(255) DEFAULT NULL COMMENT '前端组件路径',
  `redirect` VARCHAR(255) DEFAULT NULL COMMENT '重定向路径',
  `icon` VARCHAR(64) DEFAULT NULL COMMENT '图标名称',
  `menu_type` ENUM('dir','menu','button','api') NOT NULL DEFAULT 'menu' COMMENT '菜单类型',
  `permission_code` VARCHAR(128) DEFAULT NULL COMMENT '权限码',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序值，越小越靠前',
  `hidden` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否隐藏',
  `cacheable` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否缓存',
  `external_link` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否外链',
  `keep_alive` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '是否保活',
  `status` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT DEFAULT NULL COMMENT '更新人',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_sys_admin_menu_name` (`name`),
  KEY `idx_sys_admin_menu_parent_id` (`parent_id`),
  KEY `idx_sys_admin_menu_sort_order` (`sort_order`),
  KEY `idx_sys_admin_menu_status` (`status`),
  KEY `idx_sys_admin_menu_permission_code` (`permission_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='后台菜单表';
```

### 22.2 角色菜单关联表

```sql
CREATE TABLE `sys_admin_role_menu` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `role_id` BIGINT NOT NULL COMMENT '角色ID',
  `menu_id` BIGINT NOT NULL COMMENT '菜单ID',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_sys_admin_role_menu` (`role_id`, `menu_id`),
  KEY `idx_sys_admin_role_menu_role_id` (`role_id`),
  KEY `idx_sys_admin_role_menu_menu_id` (`menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='后台角色菜单关联表';
```

### 22.3 后台权限点表

```sql
CREATE TABLE `sys_admin_permission` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `perm_key` VARCHAR(128) NOT NULL COMMENT '权限码，例如 admin:user:edit',
  `perm_name` VARCHAR(128) NOT NULL COMMENT '权限名称',
  `perm_type` ENUM('page','button','api') NOT NULL DEFAULT 'button' COMMENT '权限类型',
  `menu_id` BIGINT DEFAULT NULL COMMENT '所属菜单ID',
  `status` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `created_by` BIGINT DEFAULT NULL COMMENT '创建人',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT DEFAULT NULL COMMENT '更新人',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '逻辑删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_sys_admin_permission_perm_key` (`perm_key`),
  KEY `idx_sys_admin_permission_menu_id` (`menu_id`),
  KEY `idx_sys_admin_permission_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='后台权限点表';
```

### 22.4 角色权限关联表

```sql
CREATE TABLE `sys_admin_role_permission` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `role_id` BIGINT NOT NULL COMMENT '角色ID',
  `permission_id` BIGINT NOT NULL COMMENT '权限ID',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_sys_admin_role_permission` (`role_id`, `permission_id`),
  KEY `idx_sys_admin_role_permission_role_id` (`role_id`),
  KEY `idx_sys_admin_role_permission_permission_id` (`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='后台角色权限关联表';
```

### 22.5 初始化菜单示例

```sql
INSERT INTO `sys_admin_menu` (`parent_id`, `name`, `title`, `path`, `component`, `icon`, `menu_type`, `permission_code`, `sort_order`, `hidden`, `cacheable`, `external_link`, `keep_alive`, `status`) VALUES
(0, 'dashboard', '仪表盘', '/admin/dashboard', 'views/DashboardView', 'Dashboard', 'menu', 'admin:dashboard:view', 1, 0, 1, 0, 1, 1),
(0, 'user', '用户管理', '/admin/users', 'views/UserListView', 'Users', 'menu', 'admin:user:list', 2, 0, 1, 0, 1, 1),
(0, 'rbac', '权限中心', '/admin/rbac', NULL, 'Shield', 'dir', NULL, 3, 0, 1, 0, 1, 1),
(3, 'role', '角色管理', '/admin/roles', 'views/RoleListView', 'Badge', 'menu', 'admin:role:list', 1, 0, 1, 0, 1, 1),
(3, 'permission', '权限管理', '/admin/permissions', 'views/PermissionListView', 'Key', 'menu', 'admin:permission:list', 2, 0, 1, 0, 1, 1),
(3, 'menu', '菜单管理', '/admin/menus', 'views/MenuListView', 'Menu', 'menu', 'admin:menu:list', 3, 0, 1, 0, 1, 1);
```

### 22.6 MySQL 落库建议

- 建议使用 MySQL 8.0+
- 统一使用 `utf8mb4`
- 如果项目已有逻辑删除规范，则 `deleted` 字段应与现有基类兼容
- 如果已有审计字段规范，应统一 `created_by`、`updated_by`、`created_at`、`updated_at`
- 如果 `SysRole`、`SysPermission` 已存在，可逐步迁移到后台专用表或复用现有表

---

## 23. 后端 `/admin` 模块的包结构和类清单

下面给出一版适合 Spring Boot 的后台模块包结构，便于后续按领域分层实现。

### 23.1 推荐包结构

```text
com.linkx.server
├── controller
│   └── admin
├── controller
│   └── admin
│       ├── dto
│       └── vo
├── service
│   └── admin
├── service
│   └── admin
│       └── impl
├── mapper
│   └── admin
├── entity
│   └── admin
├── config
│   └── admin
├── security
│   └── admin
└── common
    └── admin
```

### 23.2 具体类清单

#### 认证与会话
- `AdminAuthController`
- `AdminAuthService`
- `AdminAuthServiceImpl`
- `AdminLoginDTO`
- `AdminLoginVO`
- `AdminSessionVO`
- `AdminAuthInterceptor`
- `AdminPermissionInterceptor`

#### 仪表盘
- `AdminDashboardController`
- `AdminDashboardService`
- `AdminDashboardServiceImpl`
- `AdminDashboardSummaryVO`
- `AdminDashboardTrendVO`
- `AdminDashboardRealtimeVO`

#### 用户管理
- `AdminUserController`
- `AdminUserService`
- `AdminUserServiceImpl`
- `AdminUserQueryDTO`
- `AdminUserUpdateDTO`
- `AdminUserDetailVO`
- `AdminUserListVO`
- `AdminUserDeviceVO`
- `AdminUserLoginVO`

#### 角色与权限
- `AdminRoleController`
- `AdminRoleService`
- `AdminRoleServiceImpl`
- `AdminRoleDTO`
- `AdminRoleVO`
- `AdminPermissionController`
- `AdminPermissionService`
- `AdminPermissionServiceImpl`
- `AdminPermissionDTO`
- `AdminPermissionVO`
- `AdminMenuController`
- `AdminMenuService`
- `AdminMenuServiceImpl`
- `AdminMenuDTO`
- `AdminMenuVO`
- `AdminMenuTreeVO`

#### 日志与审计
- `AdminAuditLogController`
- `AdminAuditLogService`
- `AdminAuditLogServiceImpl`
- `AdminAuditLogQueryDTO`
- `AdminAuditLogVO`
- `AdminLoginLogController`
- `AdminLoginLogService`
- `AdminLoginLogServiceImpl`
- `AdminLoginLogQueryDTO`
- `AdminLoginLogVO`
- `AdminRiskEventController`
- `AdminRiskEventService`
- `AdminRiskEventServiceImpl`
- `AdminRiskEventVO`

#### 反馈与审核
- `AdminFeedbackController`
- `AdminFeedbackService`
- `AdminFeedbackServiceImpl`
- `AdminFeedbackQueryDTO`
- `AdminFeedbackReplyDTO`
- `AdminFeedbackVO`
- `AdminReviewController`
- `AdminReviewService`
- `AdminReviewServiceImpl`
- `AdminReviewQueryDTO`
- `AdminReviewActionDTO`
- `AdminReviewVO`

#### 运营与配置
- `AdminNoticeController`
- `AdminNoticeService`
- `AdminNoticeServiceImpl`
- `AdminNoticeDTO`
- `AdminNoticeVO`
- `AdminVersionController`
- `AdminVersionService`
- `AdminVersionServiceImpl`
- `AdminVersionDTO`
- `AdminVersionVO`
- `AdminSettingController`
- `AdminSettingService`
- `AdminSettingServiceImpl`
- `AdminSettingVO`
- `AdminSettingDTO`

#### 基础支持类
- `AdminMenuMapper`
- `AdminRoleMenuMapper`
- `AdminPermissionMapper`
- `AdminRolePermissionMapper`
- `AdminMenuEntity`
- `AdminPermissionEntity`
- `AdminRoleMenuEntity`
- `AdminRolePermissionEntity`
- `AdminPermissionCode`
- `AdminModuleConstants`
- `AdminPageResult`
- `AdminTreeBuilder`

### 23.3 分层职责建议

#### Controller 层
- 只处理入参、出参和权限注解
- 不写复杂业务逻辑
- 返回统一 `Result`

#### Service 层
- 处理核心业务流程
- 组装权限树、菜单树、统计数据
- 承担校验与事务边界

#### Mapper 层
- 负责数据库查询与持久化
- 复杂 SQL 与树查询可放在这里

#### Entity 层
- 对应数据库表
- 统一审计字段和逻辑删除字段

#### DTO/VO 层
- DTO：入参对象
- VO：出参对象
- 不直接暴露 Entity 到前端

### 23.4 建议的模块拆分顺序

建议优先实现：

1. `admin.auth`
2. `admin.menu`
3. `admin.role`
4. `admin.permission`
5. `admin.user`
6. `admin.audit`
7. `admin.feedback`
8. `admin.review`
9. `admin.notice`
10. `admin.version`
11. `admin.setting`
12. `admin.statistics`

### 23.5 推荐的接口路径映射

- ✅ `AdminAuthController` → `/admin/auth/**`
- ✅ `AdminMenuController` → `/admin/menus/**`
- ✅ `AdminRoleController` → `/admin/roles/**`
- ✅ `AdminPermissionController` → `/admin/permissions/**`
- ✅ `AdminUserController` → `/admin/users/**`
- ✅ `AdminAuditLogController` → `/admin/audit-logs/**`
- ✅ `AdminLoginLogController` → `/admin/login-logs/**`
- ✅ `AdminFeedbackController` → `/admin/feedback/**`
- ✅ `AdminReviewController` → `/admin/reviews/**`
- ✅ `AdminRiskEventController` → `/admin/risk-events/**`
- ✅ `AdminNoticeController` → `/admin/notices/**`
- ❌ `AdminVersionController` → `/admin/versions/**`（未建；版本走 Setting）
- ✅ `AdminSettingController` → `/admin/settings/**`
- ✅ `AdminDashboardController` → `/admin/dashboard/**`
- ✅ 另已落地：`AdminBlacklistController`、`AdminDeviceController`、`AdminSensitiveWordController`、`AdminBannerController`、`AdminStatisticsController`、`AdminEventsController`

---

## 24. `/admin` 模块的 Swagger 接口文档草案

下面给出一版适合直接落到 Swagger/OpenAPI 注解上的接口文档草案。该草案按模块划分，便于前后端联调和后续自动生成文档。

### 24.1 认证模块

#### `POST /admin/auth/login`

**说明**：管理员登录。

**请求体**：

```json
{
  "username": "admin",
  "password": "******",
  "captchaId": "captcha-uuid",
  "captchaCode": "1234"
}
```

**响应**：

```json
{
  "accessToken": "eyJhbGciOi...",
  "refreshToken": "eyJhbGciOi...",
  "expiresIn": 7200,
  "user": {
    "id": 1,
    "username": "admin",
    "nickname": "超级管理员",
    "avatar": "https://...",
    "roles": ["SUPER_ADMIN"],
    "permissions": ["admin:*"]
  }
}
```

**关键字段**：
- `username`：管理员账号
- `password`：登录密码
- `captchaId` / `captchaCode`：验证码信息

---

#### `GET /admin/auth/me`

**说明**：获取当前登录管理员信息。

**响应**：

```json
{
  "id": 1,
  "username": "admin",
  "nickname": "超级管理员",
  "email": "admin@example.com",
  "roles": ["SUPER_ADMIN"],
  "permissions": ["admin:dashboard:view", "admin:user:list"]
}
```

---

#### `GET /admin/auth/menus`

**说明**：获取当前管理员可见菜单树。

**响应**：

```json
[
  {
    "id": 1,
    "title": "仪表盘",
    "path": "/admin/dashboard",
    "icon": "Dashboard",
    "children": []
  }
]
```

---

#### `GET /admin/auth/permissions`

**说明**：获取当前管理员权限码集合。

**响应**：

```json
[
  "admin:dashboard:view",
  "admin:user:list",
  "admin:user:edit"
]
```

---

#### `POST /admin/auth/logout`

**说明**：退出登录，清理会话与 Token 状态。

---

#### `POST /admin/auth/refresh`

**说明**：使用 refresh token 刷新 access token。

---

### 24.2 仪表盘模块

#### `GET /admin/dashboard/summary`

**说明**：返回仪表盘核心统计卡片。

**响应**：

```json
{
  "totalUsers": 10240,
  "activeUsers": 5321,
  "onlineDevices": 881,
  "pendingFeedback": 14,
  "pendingReviews": 7,
  "riskEvents": 3
}
```

---

#### `GET /admin/dashboard/trends`

**说明**：返回趋势图数据。

**响应**：

```json
{
  "labels": ["07-24", "07-25", "07-26"],
  "series": [
    {
      "name": "新增用户",
      "data": [120, 132, 98]
    }
  ]
}
```

---

#### `GET /admin/dashboard/realtime`

**说明**：返回实时在线与风控状态。

---

#### `GET /admin/dashboard/pending-tasks`

**说明**：返回待处理任务列表。

---

### 24.3 用户管理模块

#### `GET /admin/users`

**说明**：分页查询用户列表。

**查询参数**：
- `page`
- `size`
- `keyword`
- `status`
- `startTime`
- `endTime`

---

#### `GET /admin/users/{id}`

**说明**：查询用户详情。

---

#### `PUT /admin/users/{id}`

**说明**：更新用户资料。

**请求体**：

```json
{
  "nickname": "张三",
  "avatar": "https://...",
  "signature": "新的签名"
}
```

---

#### `POST /admin/users/{id}/freeze`
#### `POST /admin/users/{id}/unfreeze`
#### `POST /admin/users/{id}/ban`
#### `POST /admin/users/{id}/unban`

**说明**：账号冻结、解冻、封禁、解封。

---

#### `GET /admin/users/{id}/devices`

**说明**：查询用户设备列表。

---

#### `GET /admin/users/{id}/logins`

**说明**：查询用户登录记录。

---

#### `GET /admin/users/export`

**说明**：导出用户列表数据。

---

### 24.4 角色与权限模块

#### `GET /admin/roles`
#### `POST /admin/roles`
#### `PUT /admin/roles/{id}`
#### `DELETE /admin/roles/{id}`
#### `GET /admin/roles/{id}`

**说明**：角色列表、创建、更新、删除、详情。

---

#### `GET /admin/roles/{id}/menus`
#### `PUT /admin/roles/{id}/menus`

**说明**：查询并更新角色菜单授权。

---

#### `GET /admin/roles/{id}/permissions`
#### `PUT /admin/roles/{id}/permissions`

**说明**：查询并更新角色权限授权。

---

#### `GET /admin/roles/{id}/users`
#### `PUT /admin/roles/{id}/users`

**说明**：查询并更新角色绑定用户。

---

#### `GET /admin/permissions`
#### `POST /admin/permissions`
#### `PUT /admin/permissions/{id}`
#### `DELETE /admin/permissions/{id}`

**说明**：权限点管理。

---

#### `GET /admin/menus`
#### `POST /admin/menus`
#### `PUT /admin/menus/{id}`
#### `DELETE /admin/menus/{id}`
#### `POST /admin/menus/reorder`

**说明**：菜单树查询、创建、更新、删除、排序。

---

### 24.5 日志与风控模块

#### `GET /admin/audit-logs`
#### `GET /admin/audit-logs/{id}`

**说明**：查询操作日志和日志详情。

---

#### `GET /admin/login-logs`
#### `GET /admin/login-logs/{id}`

**说明**：查询登录日志和详情。

---

#### `GET /admin/risk-events`
#### `GET /admin/risk-events/{id}`
#### `POST /admin/risk-events/{id}/handle`

**说明**：查询、查看和处置风险事件。

---

### 24.6 反馈与审核模块

#### `GET /admin/feedback`
#### `GET /admin/feedback/{id}`
#### `POST /admin/feedback/{id}/reply`
#### `POST /admin/feedback/{id}/close`
#### `POST /admin/feedback/{id}/reopen`

**说明**：反馈查询与处置。

---

#### `GET /admin/reviews`
#### `GET /admin/reviews/{id}`
#### `POST /admin/reviews/{id}/approve`
#### `POST /admin/reviews/{id}/reject`
#### `POST /admin/reviews/batch`

**说明**：内容审核与批量处置。

---

### 24.7 运营模块

#### `GET /admin/notices`
#### `POST /admin/notices`
#### `PUT /admin/notices/{id}`
#### `DELETE /admin/notices/{id}`
#### `POST /admin/notices/{id}/publish`
#### `POST /admin/notices/{id}/unpublish`

**说明**：公告管理与发布控制。

---

#### `GET /admin/versions`
#### `POST /admin/versions`
#### `PUT /admin/versions/{id}`
#### `DELETE /admin/versions/{id}`
#### `POST /admin/versions/{id}/publish`

**说明**：版本管理与发布控制。

---

### 24.8 系统配置与统计模块

#### `GET /admin/settings`
#### `PUT /admin/settings`
#### `GET /admin/settings/public`
#### `GET /admin/settings/security`
#### `GET /admin/settings/upload`
#### `GET /admin/settings/notification`

**说明**：系统配置查询与保存。

---

#### `GET /admin/statistics/overview`
#### `GET /admin/statistics/users`
#### `GET /admin/statistics/content`
#### `GET /admin/statistics/risk`
#### `GET /admin/statistics/feedback`
#### `GET /admin/statistics/export`

**说明**：统计面板与导出。

---

### 24.9 Swagger 注解建议

建议按如下方式写注解：

- `@Tag(name = "管理端-用户管理")`
- `@Operation(summary = "查询用户列表")`
- `@Parameter(name = "page", description = "页码")`
- `@Schema(description = "用户详情")`
- `@ApiResponses({...})`

建议每个 Controller 都补齐：

- 接口说明
- 参数说明
- 返回结构说明
- 权限码说明
- 是否需要审计说明

---

## 25. 管理端权限码字典表

下面整理一份建议的权限码字典，便于前后端统一使用。

### 25.1 权限码命名规则

统一使用以下格式：

```text
admin:模块:动作
```

例如：

- `admin:user:list`
- `admin:user:edit`
- `admin:user:ban`
- `admin:role:list`
- `admin:menu:edit`

### 25.2 权限码字典

> 状态列：✅ 已种子化且前后端使用 · ⚠️ 权限码存在但能力合并/前端未用 · ❌ 文档有、实现未用或未落地

| 状态 | 权限码 | 名称 | 适用范围 |
|---|---|---|---|
| ✅ | `admin:dashboard:view` | 查看仪表盘 | 仪表盘页面 |
| ✅ | `admin:user:list` | 查看用户列表 | 用户管理 |
| ✅ | `admin:user:view` | 查看用户详情 | 用户详情页 |
| ✅ | `admin:user:edit` | 编辑用户资料 | 用户编辑 |
| ✅ | `admin:user:freeze` | 冻结用户 | 用户处置 |
| ✅ | `admin:user:unfreeze` | 解冻用户 | 用户处置 |
| ✅ | `admin:user:ban` | 封禁用户 | 用户处置 |
| ✅ | `admin:user:unban` | 解封用户 | 用户处置 |
| ✅ | `admin:user:export` | 导出用户数据 | 用户列表 |
| ✅ | `admin:user:device:list` | 查看用户设备 | 用户详情 |
| ✅ | `admin:user:login:list` | 查看用户登录记录 | 用户详情 |
| ✅ | `admin:role:list` | 查看角色列表 | 角色管理 |
| ⚠️ | `admin:role:view` | 查看角色详情 | 角色管理 |
| ✅ | `admin:role:create` | 新增角色 | 角色管理 |
| ✅ | `admin:role:edit` | 编辑角色 | 角色管理 |
| ✅ | `admin:role:delete` | 删除角色 | 角色管理 |
| ✅ | `admin:role:assign-menu` | 角色分配菜单 | 权限管理 |
| ❌ | `admin:role:assign-permission` | 角色分配权限 | 权限管理 |
| ✅ | `admin:role:assign-user` | 角色分配用户 | 权限管理 |
| ✅ | `admin:permission:list` | 查看权限列表 | 权限管理 |
| ⚠️ | `admin:permission:view` | 查看权限详情 | 权限管理 |
| ✅ | `admin:permission:create` | 新增权限 | 权限管理 |
| ✅ | `admin:permission:edit` | 编辑权限 | 权限管理 |
| ✅ | `admin:permission:delete` | 删除权限 | 权限管理 |
| ✅ | `admin:menu:list` | 查看菜单列表 | 菜单管理 |
| ⚠️ | `admin:menu:view` | 查看菜单详情 | 菜单管理 |
| ⚠️ | `admin:menu:create` | 新增菜单 | 菜单管理 |
| ⚠️ | `admin:menu:edit` | 编辑菜单 | 菜单管理 |
| ⚠️ | `admin:menu:delete` | 删除菜单 | 菜单管理 |
| ❌ | `admin:menu:reorder` | 调整菜单排序 | 菜单管理 |
| ✅ | `admin:audit:list` | 查看操作日志 | 日志中心 |
| ✅ | `admin:audit:view` | 查看操作日志详情 | 日志中心 |
| ✅ | `admin:login-log:list` | 查看登录日志 | 日志中心 |
| ✅ | `admin:login-log:view` | 查看登录日志详情 | 日志中心 |
| ✅ | `admin:risk-event:list` | 查看风险事件 | 风控中心 |
| ✅ | `admin:risk-event:view` | 查看风险事件详情 | 风控中心 |
| ✅ | `admin:risk-event:handle` | 处置风险事件 | 风控中心 |
| ✅ | `admin:feedback:list` | 查看反馈列表 | 反馈管理 |
| ✅ | `admin:feedback:view` | 查看反馈详情 | 反馈管理 |
| ✅ | `admin:feedback:reply` | 回复反馈 | 反馈管理 |
| ✅ | `admin:feedback:close` | 关闭反馈 | 反馈管理 |
| ✅ | `admin:feedback:reopen` | 重新打开反馈 | 反馈管理 |
| ✅ | `admin:review:list` | 查看审核列表 | 内容审核 |
| ✅ | `admin:review:view` | 查看审核详情 | 内容审核 |
| ✅ | `admin:review:approve` | 审核通过 | 内容审核 |
| ✅ | `admin:review:reject` | 审核拒绝 | 内容审核 |
| ✅ | `admin:review:batch` | 批量审核 | 内容审核 |
| ✅ | `admin:notice:list` | 查看公告列表 | 运营中心 |
| ✅ | `admin:notice:view` | 查看公告详情 | 运营中心 |
| ✅ | `admin:notice:create` | 新增公告 | 运营中心 |
| ✅ | `admin:notice:edit` | 编辑公告 | 运营中心 |
| ✅ | `admin:notice:delete` | 删除公告 | 运营中心 |
| ✅ | `admin:notice:publish` | 发布公告 | 运营中心 |
| ✅ | `admin:notice:unpublish` | 下线公告 | 运营中心 |
| ⚠️ | `admin:version:list` | 查看版本列表 | 版本管理 |
| ❌ | `admin:version:view` | 查看版本详情 | 版本管理 |
| ❌ | `admin:version:create` | 新增版本 | 版本管理 |
| ❌ | `admin:version:edit` | 编辑版本 | 版本管理 |
| ❌ | `admin:version:delete` | 删除版本 | 版本管理 |
| ❌ | `admin:version:publish` | 发布版本 | 版本管理 |
| ✅ | `admin:setting:view` | 查看系统配置 | 系统配置 |
| ✅ | `admin:setting:edit` | 编辑系统配置 | 系统配置 |
| ✅ | `admin:statistics:view` | 查看统计分析 | 统计中心 |
| ✅ | `admin:statistics:export` | 导出统计数据 | 统计中心 |
| ✅ | `admin:auth:login` | 管理员登录 | 认证 |
| ✅ | `admin:auth:logout` | 管理员退出 | 认证 |
| ✅ | `admin:auth:refresh` | 刷新令牌 | 认证 |


### 25.2.1 额外已落地权限码（文档原表未列全）

- ✅ `admin:user:reset-password`
- ✅ `admin:blacklist:list` / `add` / `remove`
- ✅ `admin:device:list` / `kick`
- ✅ `admin:sensitive-word:list` / `create` / `edit` / `delete`
- ✅ `admin:banner:list` / `view` / `create` / `edit` / `delete` / `publish` / `unpublish`
- ✅ `admin:notice:inbox`
- ✅ `admin:audit:export` / `admin:login-log:export` / `admin:risk-event:export` / `admin:risk-event:batch`
- ✅ `admin:feedback:export` / `admin:review:export`

### 25.3 推荐补充的公共权限码

如果后续需要更细粒度控制，可继续补充：

- `admin:file:download`
- `admin:file:upload`
- `admin:config:system`
- `admin:config:security`
- `admin:config:upload`
- `admin:config:notification`

### 25.4 使用建议

- 页面权限用于菜单显示
- 按钮权限用于控制新增、编辑、删除、发布等操作按钮
- 接口权限用于后端鉴权与安全隔离
- 建议权限码在前后端保持完全一致
- 权限码一旦确定，尽量不要频繁改名，避免历史角色失效

---

## 26. 管理端权限码初始化 SQL

下面给出一版权限码初始化 SQL，可作为后台权限字典的初始数据。建议先插入菜单权限与按钮权限，再由角色表进行授权。

### 26.1 权限码初始化数据

```sql
INSERT INTO `sys_admin_permission` (`perm_key`, `perm_name`, `perm_type`, `menu_id`, `status`, `remark`, `created_at`, `updated_at`, `deleted`) VALUES
('admin:dashboard:view', '查看仪表盘', 'page', NULL, 1, '仪表盘首页权限', NOW(), NOW(), 0),
('admin:user:list', '查看用户列表', 'page', NULL, 1, '用户管理列表页权限', NOW(), NOW(), 0),
('admin:user:view', '查看用户详情', 'button', NULL, 1, '用户详情查看权限', NOW(), NOW(), 0),
('admin:user:edit', '编辑用户资料', 'button', NULL, 1, '用户资料编辑权限', NOW(), NOW(), 0),
('admin:user:freeze', '冻结用户', 'button', NULL, 1, '用户冻结权限', NOW(), NOW(), 0),
('admin:user:unfreeze', '解冻用户', 'button', NULL, 1, '用户解冻权限', NOW(), NOW(), 0),
('admin:user:ban', '封禁用户', 'button', NULL, 1, '用户封禁权限', NOW(), NOW(), 0),
('admin:user:unban', '解封用户', 'button', NULL, 1, '用户解封权限', NOW(), NOW(), 0),
('admin:user:export', '导出用户数据', 'button', NULL, 1, '用户导出权限', NOW(), NOW(), 0),
('admin:user:device:list', '查看用户设备', 'button', NULL, 1, '用户设备查看权限', NOW(), NOW(), 0),
('admin:user:login:list', '查看用户登录记录', 'button', NULL, 1, '用户登录记录查看权限', NOW(), NOW(), 0),
('admin:role:list', '查看角色列表', 'page', NULL, 1, '角色管理列表权限', NOW(), NOW(), 0),
('admin:role:view', '查看角色详情', 'button', NULL, 1, '角色详情权限', NOW(), NOW(), 0),
('admin:role:create', '新增角色', 'button', NULL, 1, '新增角色权限', NOW(), NOW(), 0),
('admin:role:edit', '编辑角色', 'button', NULL, 1, '编辑角色权限', NOW(), NOW(), 0),
('admin:role:delete', '删除角色', 'button', NULL, 1, '删除角色权限', NOW(), NOW(), 0),
('admin:role:assign-menu', '角色分配菜单', 'button', NULL, 1, '角色菜单授权权限', NOW(), NOW(), 0),
('admin:role:assign-permission', '角色分配权限', 'button', NULL, 1, '角色权限授权权限', NOW(), NOW(), 0),
('admin:role:assign-user', '角色分配用户', 'button', NULL, 1, '角色绑定用户权限', NOW(), NOW(), 0),
('admin:permission:list', '查看权限列表', 'page', NULL, 1, '权限列表页权限', NOW(), NOW(), 0),
('admin:permission:view', '查看权限详情', 'button', NULL, 1, '权限详情查看权限', NOW(), NOW(), 0),
('admin:permission:create', '新增权限', 'button', NULL, 1, '新增权限点权限', NOW(), NOW(), 0),
('admin:permission:edit', '编辑权限', 'button', NULL, 1, '编辑权限点权限', NOW(), NOW(), 0),
('admin:permission:delete', '删除权限', 'button', NULL, 1, '删除权限点权限', NOW(), NOW(), 0),
('admin:menu:list', '查看菜单列表', 'page', NULL, 1, '菜单列表页权限', NOW(), NOW(), 0),
('admin:menu:view', '查看菜单详情', 'button', NULL, 1, '菜单详情查看权限', NOW(), NOW(), 0),
('admin:menu:create', '新增菜单', 'button', NULL, 1, '新增菜单权限', NOW(), NOW(), 0),
('admin:menu:edit', '编辑菜单', 'button', NULL, 1, '编辑菜单权限', NOW(), NOW(), 0),
('admin:menu:delete', '删除菜单', 'button', NULL, 1, '删除菜单权限', NOW(), NOW(), 0),
('admin:menu:reorder', '调整菜单排序', 'button', NULL, 1, '菜单拖拽排序权限', NOW(), NOW(), 0),
('admin:audit:list', '查看操作日志', 'page', NULL, 1, '操作日志列表权限', NOW(), NOW(), 0),
('admin:audit:view', '查看操作日志详情', 'button', NULL, 1, '操作日志详情查看权限', NOW(), NOW(), 0),
('admin:login-log:list', '查看登录日志', 'page', NULL, 1, '登录日志列表权限', NOW(), NOW(), 0),
('admin:login-log:view', '查看登录日志详情', 'button', NULL, 1, '登录日志详情查看权限', NOW(), NOW(), 0),
('admin:risk-event:list', '查看风险事件', 'page', NULL, 1, '风险事件列表权限', NOW(), NOW(), 0),
('admin:risk-event:view', '查看风险事件详情', 'button', NULL, 1, '风险事件详情查看权限', NOW(), NOW(), 0),
('admin:risk-event:handle', '处置风险事件', 'button', NULL, 1, '风险事件处置权限', NOW(), NOW(), 0),
('admin:feedback:list', '查看反馈列表', 'page', NULL, 1, '反馈列表页权限', NOW(), NOW(), 0),
('admin:feedback:view', '查看反馈详情', 'button', NULL, 1, '反馈详情查看权限', NOW(), NOW(), 0),
('admin:feedback:reply', '回复反馈', 'button', NULL, 1, '反馈回复权限', NOW(), NOW(), 0),
('admin:feedback:close', '关闭反馈', 'button', NULL, 1, '反馈关闭权限', NOW(), NOW(), 0),
('admin:feedback:reopen', '重新打开反馈', 'button', NULL, 1, '反馈重开权限', NOW(), NOW(), 0),
('admin:review:list', '查看审核列表', 'page', NULL, 1, '审核列表页权限', NOW(), NOW(), 0),
('admin:review:view', '查看审核详情', 'button', NULL, 1, '审核详情查看权限', NOW(), NOW(), 0),
('admin:review:approve', '审核通过', 'button', NULL, 1, '审核通过权限', NOW(), NOW(), 0),
('admin:review:reject', '审核拒绝', 'button', NULL, 1, '审核拒绝权限', NOW(), NOW(), 0),
('admin:review:batch', '批量审核', 'button', NULL, 1, '批量审核权限', NOW(), NOW(), 0),
('admin:notice:list', '查看公告列表', 'page', NULL, 1, '公告列表页权限', NOW(), NOW(), 0),
('admin:notice:view', '查看公告详情', 'button', NULL, 1, '公告详情查看权限', NOW(), NOW(), 0),
('admin:notice:create', '新增公告', 'button', NULL, 1, '新增公告权限', NOW(), NOW(), 0),
('admin:notice:edit', '编辑公告', 'button', NULL, 1, '编辑公告权限', NOW(), NOW(), 0),
('admin:notice:delete', '删除公告', 'button', NULL, 1, '删除公告权限', NOW(), NOW(), 0),
('admin:notice:publish', '发布公告', 'button', NULL, 1, '公告发布权限', NOW(), NOW(), 0),
('admin:notice:unpublish', '下线公告', 'button', NULL, 1, '公告下线权限', NOW(), NOW(), 0),
('admin:version:list', '查看版本列表', 'page', NULL, 1, '版本列表页权限', NOW(), NOW(), 0),
('admin:version:view', '查看版本详情', 'button', NULL, 1, '版本详情查看权限', NOW(), NOW(), 0),
('admin:version:create', '新增版本', 'button', NULL, 1, '新增版本权限', NOW(), NOW(), 0),
('admin:version:edit', '编辑版本', 'button', NULL, 1, '编辑版本权限', NOW(), NOW(), 0),
('admin:version:delete', '删除版本', 'button', NULL, 1, '删除版本权限', NOW(), NOW(), 0),
('admin:version:publish', '发布版本', 'button', NULL, 1, '版本发布权限', NOW(), NOW(), 0),
('admin:setting:view', '查看系统配置', 'page', NULL, 1, '系统配置查看权限', NOW(), NOW(), 0),
('admin:setting:edit', '编辑系统配置', 'button', NULL, 1, '系统配置编辑权限', NOW(), NOW(), 0),
('admin:statistics:view', '查看统计分析', 'page', NULL, 1, '统计分析查看权限', NOW(), NOW(), 0),
('admin:statistics:export', '导出统计数据', 'button', NULL, 1, '统计数据导出权限', NOW(), NOW(), 0),
('admin:auth:login', '管理员登录', 'api', NULL, 1, '后台登录接口权限', NOW(), NOW(), 0),
('admin:auth:logout', '管理员退出', 'api', NULL, 1, '后台退出接口权限', NOW(), NOW(), 0),
('admin:auth:refresh', '刷新令牌', 'api', NULL, 1, '后台刷新令牌接口权限', NOW(), NOW(), 0);
```

### 26.2 角色默认授权建议

建议初始化时至少准备以下三类默认角色授权：

- 超级管理员：全部权限
- 运营管理员：仪表盘、用户查看、反馈、公告、版本、统计
- 审核管理员：用户处置、审核、风控、日志查看

### 26.3 初始化注意事项

- `menu_id` 可以在菜单表初始化后补齐
- 若权限点需要严格挂靠菜单，可先插入菜单再回填 `menu_id`
- 如果项目当前已存在 `SysPermission`，可迁移为后台权限码或通过同步脚本导入
- 权限码需与前端按钮权限指令保持一致

---

## 27. 后端 `/admin` 模块的实体、DTO、VO 清单

下面按模块整理一份完整的实体、DTO、VO 清单，方便你后续逐个创建文件。

### 27.1 认证模块

#### Entity
- `AdminLoginSession`
- `AdminRefreshToken`

#### DTO
- `AdminLoginDTO`
- `AdminRefreshDTO`
- `AdminLogoutDTO`

#### VO
- `AdminLoginVO`
- `AdminUserProfileVO`
- `AdminSessionVO`

---

### 27.2 仪表盘模块

#### Entity
- `AdminDashboardSnapshot`
- `AdminRealtimeMetric`

#### DTO
- `AdminDashboardQueryDTO`

#### VO
- `AdminDashboardSummaryVO`
- `AdminDashboardTrendVO`
- `AdminDashboardRealtimeVO`
- `AdminPendingTaskVO`

---

### 27.3 用户管理模块

#### Entity
- `AdminUserView`
- `AdminUserDevice`
- `AdminUserLoginRecord`
- `AdminUserBlacklist`

#### DTO
- `AdminUserQueryDTO`
- `AdminUserUpdateDTO`
- `AdminUserFreezeDTO`
- `AdminUserBanDTO`
- `AdminUserExportDTO`

#### VO
- `AdminUserListVO`
- `AdminUserDetailVO`
- `AdminUserDeviceVO`
- `AdminUserLoginVO`
- `AdminUserExportVO`

---

### 27.4 角色与权限模块

#### Entity
- `AdminRoleMenu`
- `AdminRolePermission`
- `AdminPermission`
- `AdminMenu`

#### DTO
- `AdminRoleDTO`
- `AdminRoleUpdateDTO`
- `AdminRoleAssignMenuDTO`
- `AdminRoleAssignPermissionDTO`
- `AdminRoleAssignUserDTO`
- `AdminPermissionDTO`
- `AdminPermissionUpdateDTO`
- `AdminMenuDTO`
- `AdminMenuUpdateDTO`
- `AdminMenuReorderDTO`

#### VO
- `AdminRoleVO`
- `AdminPermissionVO`
- `AdminMenuVO`
- `AdminMenuTreeVO`
- `AdminRouteVO`
- `AdminRouteMetaVO`

---

### 27.5 日志与风控模块

#### Entity
- `AdminOperationLog`
- `AdminLoginLog`
- `AdminRiskEvent`

#### DTO
- `AdminOperationLogQueryDTO`
- `AdminLoginLogQueryDTO`
- `AdminRiskEventQueryDTO`
- `AdminRiskEventHandleDTO`

#### VO
- `AdminOperationLogVO`
- `AdminLoginLogVO`
- `AdminRiskEventVO`
- `AdminRiskEventDetailVO`

---

### 27.6 反馈与审核模块

#### Entity
- `AdminFeedbackReply`
- `AdminReviewTask`
- `AdminReviewRecord`

#### DTO
- `AdminFeedbackQueryDTO`
- `AdminFeedbackReplyDTO`
- `AdminFeedbackCloseDTO`
- `AdminReviewQueryDTO`
- `AdminReviewActionDTO`
- `AdminReviewBatchDTO`

#### VO
- `AdminFeedbackVO`
- `AdminFeedbackDetailVO`
- `AdminReviewVO`
- `AdminReviewDetailVO`

---

### 27.7 运营与配置模块

#### Entity
- `AdminNotice`
- `AdminVersion`
- `AdminSetting`
- `AdminBanner`

#### DTO
- `AdminNoticeDTO`
- `AdminNoticePublishDTO`
- `AdminVersionDTO`
- `AdminVersionPublishDTO`
- `AdminSettingDTO`
- `AdminBannerDTO`

#### VO
- `AdminNoticeVO`
- `AdminVersionVO`
- `AdminSettingVO`
- `AdminBannerVO`

---

### 27.8 统计模块

#### Entity
- `AdminStatisticSnapshot`
- `AdminStatisticSeries`

#### DTO
- `AdminStatisticQueryDTO`

#### VO
- `AdminStatisticOverviewVO`
- `AdminStatisticUserVO`
- `AdminStatisticContentVO`
- `AdminStatisticRiskVO`
- `AdminStatisticFeedbackVO`
- `AdminStatisticExportVO`

---

### 27.9 通用基础对象

#### DTO
- `PageQueryDTO`
- `SortQueryDTO`
- `IdListDTO`
- `StatusUpdateDTO`
- `TimeRangeQueryDTO`

#### VO
- `PageResultVO<T>`
- `TreeNodeVO<T>`
- `OptionVO`
- `KeyValueVO`
- `SelectTreeVO`

---

### 27.10 建议字段规范

#### DTO 通用字段
- `id`
- `ids`
- `keyword`
- `status`
- `startTime`
- `endTime`
- `page`
- `size`
- `sortBy`
- `sortOrder`

#### VO 通用字段
- `id`
- `name`
- `title`
- `status`
- `createdAt`
- `updatedAt`
- `children`

### 27.11 建议优先创建顺序

建议按以下顺序落地这些类：

1. 认证模块 DTO / VO
2. 菜单与权限模块实体和 VO
3. 用户管理 DTO / VO
4. 日志与风控 DTO / VO
5. 反馈与审核 DTO / VO
6. 运营与配置 DTO / VO
7. 统计模块 VO

---

## 28. 管理端第一批接口的 Swagger 注解模板

下面给出第一批最关键接口的 Swagger/OpenAPI 注解模板。可以直接作为后端控制器编写时的参考。

### 28.1 通用注解写法

建议统一使用以下注解组合：

- `@Tag`：标记模块
- `@Operation`：描述接口用途
- `@Parameter`：描述查询参数或路径参数
- `@RequestBody` + `@Schema`：描述请求体
- `@ApiResponses`：描述响应
- `@SecurityRequirement`：标记需要登录鉴权

---

### 28.2 认证接口模板

#### `AdminAuthController`

```java
@Tag(name = "管理端-认证")
@RestController
@RequestMapping("/admin/auth")
public class AdminAuthController {

    @Operation(summary = "管理员登录", description = "使用管理员账号密码登录后台")
    @PostMapping("/login")
    public Result<AdminLoginVO> login(@Valid @RequestBody AdminLoginDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "获取当前管理员信息", description = "返回当前登录用户资料、角色和权限")
    @GetMapping("/me")
    public Result<AdminUserProfileVO> me() {
        return Result.success(null);
    }

    @Operation(summary = "获取当前管理员菜单", description = "返回当前登录用户可见菜单树")
    @GetMapping("/menus")
    public Result<List<AdminMenuTreeVO>> menus() {
        return Result.success(Collections.emptyList());
    }

    @Operation(summary = "获取当前管理员权限码", description = "返回当前登录用户权限码集合")
    @GetMapping("/permissions")
    public Result<Set<String>> permissions() {
        return Result.success(Collections.emptySet());
    }

    @Operation(summary = "管理员退出登录", description = "清理后台会话与令牌")
    @PostMapping("/logout")
    public Result<Void> logout(@Valid @RequestBody AdminLogoutDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "刷新管理员令牌", description = "使用 refresh token 刷新 access token")
    @PostMapping("/refresh")
    public Result<AdminLoginVO> refresh(@Valid @RequestBody AdminRefreshDTO dto) {
        return Result.success(null);
    }
}
```

#### DTO 注解建议

```java
@Schema(description = "管理员登录请求")
public class AdminLoginDTO {

    @Schema(description = "管理员账号", example = "admin")
    @NotBlank
    private String username;

    @Schema(description = "登录密码", example = "******")
    @NotBlank
    private String password;

    @Schema(description = "验证码ID", example = "captcha-uuid")
    private String captchaId;

    @Schema(description = "验证码内容", example = "1234")
    private String captchaCode;
}
```

---

### 28.3 菜单接口模板

#### `AdminMenuController`

```java
@Tag(name = "管理端-菜单管理")
@RestController
@RequestMapping("/admin/menus")
public class AdminMenuController {

    @Operation(summary = "查询菜单树", description = "返回完整的后台菜单树")
    @GetMapping
    @PreAuthorize("hasAuthority('admin:menu:list')")
    public Result<List<AdminMenuTreeVO>> list() {
        return Result.success(Collections.emptyList());
    }

    @Operation(summary = "查询菜单详情", description = "根据菜单ID查询详情")
    @GetMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:menu:view')")
    public Result<AdminMenuVO> detail(@Parameter(description = "菜单ID") @PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "新增菜单", description = "创建新的后台菜单或权限节点")
    @PostMapping
    @PreAuthorize("hasAuthority('admin:menu:create')")
    public Result<Long> create(@Valid @RequestBody AdminMenuDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "编辑菜单", description = "更新菜单信息")
    @PutMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:menu:edit')")
    public Result<Void> update(@Parameter(description = "菜单ID") @PathVariable Long id,
                               @Valid @RequestBody AdminMenuUpdateDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "删除菜单", description = "逻辑删除菜单")
    @DeleteMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:menu:delete')")
    public Result<Void> delete(@Parameter(description = "菜单ID") @PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "调整菜单排序", description = "批量调整菜单顺序")
    @PostMapping("/reorder")
    @PreAuthorize("hasAuthority('admin:menu:reorder')")
    public Result<Void> reorder(@Valid @RequestBody AdminMenuReorderDTO dto) {
        return Result.success(null);
    }
}
```

---

### 28.4 用户管理接口模板

#### `AdminUserController`

```java
@Tag(name = "管理端-用户管理")
@RestController
@RequestMapping("/admin/users")
public class AdminUserController {

    @Operation(summary = "查询用户列表", description = "分页查询用户数据")
    @GetMapping
    @PreAuthorize("hasAuthority('admin:user:list')")
    public Result<PageResultVO<AdminUserListVO>> list(@Valid AdminUserQueryDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "查询用户详情", description = "根据用户ID查询详细资料")
    @GetMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:user:view')")
    public Result<AdminUserDetailVO> detail(@PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "更新用户资料", description = "修改用户基础信息")
    @PutMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:user:edit')")
    public Result<Void> update(@PathVariable Long id, @Valid @RequestBody AdminUserUpdateDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "冻结用户", description = "将用户置为冻结状态")
    @PostMapping("/{id}/freeze")
    @PreAuthorize("hasAuthority('admin:user:freeze')")
    public Result<Void> freeze(@PathVariable Long id, @Valid @RequestBody AdminUserFreezeDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "解冻用户", description = "解除用户冻结状态")
    @PostMapping("/{id}/unfreeze")
    @PreAuthorize("hasAuthority('admin:user:unfreeze')")
    public Result<Void> unfreeze(@PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "封禁用户", description = "将用户加入黑名单或封禁状态")
    @PostMapping("/{id}/ban")
    @PreAuthorize("hasAuthority('admin:user:ban')")
    public Result<Void> ban(@PathVariable Long id, @Valid @RequestBody AdminUserBanDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "解封用户", description = "解除用户封禁状态")
    @PostMapping("/{id}/unban")
    @PreAuthorize("hasAuthority('admin:user:unban')")
    public Result<Void> unban(@PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "查询用户设备", description = "查看用户关联设备列表")
    @GetMapping("/{id}/devices")
    @PreAuthorize("hasAuthority('admin:user:device:list')")
    public Result<List<AdminUserDeviceVO>> devices(@PathVariable Long id) {
        return Result.success(Collections.emptyList());
    }

    @Operation(summary = "查询用户登录记录", description = "查看用户登录历史")
    @GetMapping("/{id}/logins")
    @PreAuthorize("hasAuthority('admin:user:login:list')")
    public Result<List<AdminUserLoginVO>> logins(@PathVariable Long id) {
        return Result.success(Collections.emptyList());
    }
}
```

---

### 28.5 角色权限接口模板

#### `AdminRoleController`

```java
@Tag(name = "管理端-角色管理")
@RestController
@RequestMapping("/admin/roles")
public class AdminRoleController {

    @Operation(summary = "查询角色列表", description = "分页查询后台角色")
    @GetMapping
    @PreAuthorize("hasAuthority('admin:role:list')")
    public Result<PageResultVO<AdminRoleVO>> list(@Valid PageQueryDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "新增角色", description = "创建后台角色")
    @PostMapping
    @PreAuthorize("hasAuthority('admin:role:create')")
    public Result<Long> create(@Valid @RequestBody AdminRoleDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "编辑角色", description = "修改后台角色")
    @PutMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:role:edit')")
    public Result<Void> update(@PathVariable Long id, @Valid @RequestBody AdminRoleUpdateDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "删除角色", description = "删除后台角色")
    @DeleteMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:role:delete')")
    public Result<Void> delete(@PathVariable Long id) {
        return Result.success(null);
    }

    @Operation(summary = "角色菜单授权", description = "配置角色可访问菜单")
    @PutMapping("/{id}/menus")
    @PreAuthorize("hasAuthority('admin:role:assign-menu')")
    public Result<Void> assignMenus(@PathVariable Long id, @Valid @RequestBody AdminRoleAssignMenuDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "角色权限授权", description = "配置角色权限点")
    @PutMapping("/{id}/permissions")
    @PreAuthorize("hasAuthority('admin:role:assign-permission')")
    public Result<Void> assignPermissions(@PathVariable Long id, @Valid @RequestBody AdminRoleAssignPermissionDTO dto) {
        return Result.success(null);
    }
}
```

---

### 28.6 日志接口模板

#### `AdminAuditLogController`

```java
@Tag(name = "管理端-操作日志")
@RestController
@RequestMapping("/admin/audit-logs")
public class AdminAuditLogController {

    @Operation(summary = "查询操作日志", description = "分页查询后台操作日志")
    @GetMapping
    @PreAuthorize("hasAuthority('admin:audit:list')")
    public Result<PageResultVO<AdminOperationLogVO>> list(@Valid AdminOperationLogQueryDTO dto) {
        return Result.success(null);
    }

    @Operation(summary = "查询操作日志详情", description = "查看日志详情")
    @GetMapping("/{id}")
    @PreAuthorize("hasAuthority('admin:audit:view')")
    public Result<AdminOperationLogVO> detail(@PathVariable Long id) {
        return Result.success(null);
    }
}
```

---

### 28.7 Swagger 注解落地规则

建议统一：

- Controller 级别写 `@Tag`
- 每个方法写 `@Operation`
- 路径参数写 `@Parameter`
- 请求体 DTO 写 `@Schema`
- 返回 VO 写 `@Schema`
- 需要授权的接口写 `@PreAuthorize`
- 有审计动作的接口在描述里注明“写审计日志”

---

### 28.8 第一批优先落地接口

建议第一批先写这几个模块：

1. `AdminAuthController`
2. `AdminMenuController`
3. `AdminRoleController`
4. `AdminUserController`
5. `AdminAuditLogController`

这五个模块能快速打通后台登录、菜单、权限和基础管理闭环。

---

## 29. 小结

这份文档已补齐权限码初始化 SQL、`/admin` 模块实体 DTO VO 清单，以及第一批接口的 Swagger 注解模板；对应实现（登录、菜单、角色、用户、日志及后续运营治理能力）**已落地，M1–M4 已交付**。后续以本文档作对照清单做缺口核对与 M5 验收，而不是重新进入“先跑通骨架”阶段。

---

## 30. 实施版总原则

这一节将本文档从“方案说明”补成“可直接执行的实施版”。**第一版骨架与核心业务已交付**；后续增强与验收时，仍建议遵循：

1. 新增能力前先确认现有 `linkx-admin` / `/admin/**` 是否已覆盖，避免重复建设。
2. 所有列表接口统一分页、排序、筛选规范。
3. 所有写操作必须落审计日志。
4. 所有高危动作必须有二次确认与权限码校验。
5. 所有新增后台能力优先复用现有后端实体、Service、Mapper，不重复造轮子。
6. 联调优先接真实接口；仅在接口未就绪时临时 Mock。
7. 菜单、权限、角色变更需前后端同步，并纳入角色冒烟用例。

---

## 31. 现有资产复用清单

下面是管理端实施时可以直接复用的现有能力，开发时优先级应高于新增表和新增接口。

### 31.1 可直接复用的后端能力

- `SysUser`
- `SysRole`
- `SysPermission`
- `SysRolePermission`
- `SysUserRole`
- `RbacController`
- `RbacService`
- `AuditLogService`
- `LoginAuditService`
- `DeviceSessionService`
- `PresenceService`
- `FeedbackController`
- `SensitiveWordService`
- `ComplianceController`
- `VersionController`
- `LinkxProperties`

### 31.2 建议复用策略

- 用户管理：优先复用 `SysUser` 与 `UserController` 相关查询能力。
- 角色与权限：优先复用现有 `SysRole` / `SysPermission` / `RbacService`。
- 登录与设备：优先复用 `LoginAuditService` 与 `DeviceSessionService`。
- 内容治理：优先复用 `ComplianceController` 与 `SensitiveWordService`。
- 反馈与运营：优先复用 `FeedbackController`、`VersionController`。

### 31.3 不建议立即新增的内容

以下内容如果现有后端已经能覆盖，不建议第一版就重建：

- 重复的管理员账号体系
- 重复的登录审计体系
- 重复的权限模型
- 重复的文件存储与导出体系

---

## 32. 第一版实施范围

> **第一版（对应 M1–M4）已交付。** 本节保留为交付范围说明与对照清单。

### 32.1 必做范围 ✅ 已交付

1. ✅ 管理端登录、退出、刷新会话
2. ✅ 菜单权限加载
3. ✅ 仪表盘基础统计
4. ✅ 用户列表与详情
5. ✅ 角色、权限、菜单管理（菜单前端只读）
6. ✅ 操作日志、登录日志
7. ✅ 反馈管理
8. ✅ 内容审核基础列表
9. ✅ 系统配置
10. ✅ 版本管理（并入系统配置）

### 32.2 暂缓范围

以下能力仍建议进入第二版或后续迭代（M5 之后）：

- ❌ 复杂的风控模型配置
- ❌ 细粒度数据权限分组（部门/组织级）
- ❌ 复杂审批流
- ❌ 高级 BI 看板
- ❌ 多租户能力
- ❌ 移动端管理后台
- ❌ 版本强更 / 灰度发布产品化

### 32.3 第一版交付目标 ✅ 已达成（需持续回归）

第一版已满足：

- ✅ 运营人员能登录后台并看到菜单
- ✅ 审核人员能处理反馈与违规内容
- ✅ 管理员能查询用户、角色、权限和日志
- ✅ 关键操作进入审计记录
- ✅ 权限按角色隔离（含运营/审核/安全/只读等种子角色）

当前应在此基础上做 M5 回归与生产加固，而不是重新立项第一版。

---

## 33. 后端落地实施步骤

建议按以下顺序实现后端，避免前后端阻塞。

### 33.1 第一步：认证与会话 ✅

#### 需要完成

- ✅ 新增 `AdminAuthController`
- ✅ 新增 `AdminAuthService`
- ✅ 打通登录、退出、刷新接口
- ✅ 返回管理员信息、角色和权限码
- ✅ 接入验证码校验
- ✅ 接入 Token 持久化或黑名单机制

#### 验收标准

- ✅ 能使用管理员账号登录后台
- ✅ 登录后能拿到 `accessToken` 和 `refreshToken`
- ✅ Token 失效后能自动跳转登录页
- ✅ 退出登录后旧 Token 立即不可用

### 33.2 第二步：菜单与权限 ✅（菜单前端写操作待补）

#### 需要完成

- ✅ 新增 `AdminMenuController`
- ✅ 新增 `AdminPermissionController`
- ✅ 新增 `AdminRoleController`
- ✅ 菜单树查询接口
- ✅ 角色菜单授权接口
- ⚠️ 角色权限授权接口（无独立 permissions 绑定 API；靠菜单 + 权限点管理）
- ✅ 路由守卫接入

#### 验收标准

- ✅ 不同角色登录后看到不同菜单
- ✅ 无权限页面自动拦截
- ✅ 按钮级权限能隐藏新增、删除、发布等操作

### 33.3 第三步：用户管理 ✅

#### 需要完成

- ✅ 用户列表分页
- ✅ 用户详情页
- ✅ 设备列表
- ✅ 登录记录
- ✅ 冻结、解冻、封禁、解封
- ✅ 导出用户数据

#### 验收标准

- 可按关键词、状态、时间范围检索用户
- 用户详情信息完整
- 管理员执行处置动作后有审计记录

### 33.4 第四步：审计与日志 ✅

#### 需要完成

- ✅ 操作日志查询
- ✅ 登录日志查询
- ✅ 风险事件列表
- ✅ 风险事件处置
- ✅ 日志详情页

#### 验收标准

- 所有关键操作都能检索到日志
- 日志支持按时间、操作人、模块筛选
- 高危事件有明确处理状态

### 33.5 第五步：反馈与审核 ✅

#### 需要完成

- ✅ 反馈列表与详情
- ✅ 回复反馈
- ✅ 关闭与重开反馈
- ✅ 违规内容列表
- ✅ 审核通过 / 驳回

#### 验收标准

- 反馈能闭环处理
- 审核动作能留下审核意见和时间
- 批量审核不会丢失单条失败信息

### 33.6 第六步：运营与配置 ✅

#### 需要完成

- ✅ 公告管理
- ⚠️ 版本管理（并入系统配置）
- ✅ 系统配置页面
- ✅ 配置保存与生效

#### 验收标准

- 公告可发布、下线
- 版本可发布并可控制强更
- 配置修改后能在合理时间内生效

---

## 34. 前端落地实施步骤

### 34.1 第一步：工程初始化 ✅

#### 需要完成

- ✅ 创建 `linkx-admin`
- ⚠️ 配置 Vite、TypeScript、ESLint、Prettier（Vite/TS 已齐；ESLint/Prettier 视仓库配置）
- ✅ 接入 Naive UI 或 Arco Design Vue
- ✅ 接入 Pinia、Vue Router、Axios
- ✅ 配置环境变量

#### 验收标准

- 项目可启动
- 可以打包
- 基础目录结构清晰

### 34.2 第二步：基础框架 ✅

#### 需要完成

- ✅ 登录页
- ✅ 后台布局
- ✅ 侧边栏菜单
- ✅ 顶部栏
- ✅ 面包屑
- ✅ 权限守卫
- ✅ 退出登录

#### 验收标准

- 登录后能进入后台
- 刷新页面后状态不丢失
- 未登录无法访问后台页面

### 34.3 第三步：通用页面能力 ✅

#### 需要完成

- ✅ 表格分页
- ✅ 表单弹窗
- ✅ 抽屉详情
- ✅ 统一搜索栏
- ✅ 上传下载组件
- ✅ 二次确认弹窗

#### 验收标准

- 管理端列表页开发成本明显降低
- 各页面风格统一
- 危险操作必有确认

### 34.4 第四步：业务页面 ✅

按以下顺序实现：

1. ✅ 仪表盘
2. ✅ 用户管理
3. ✅ 角色权限
4. ✅ 日志中心
5. ✅ 反馈与审核
6. ✅ 运营管理
7. ✅ 系统配置
8. ✅ 统计分析

---

## 35. 数据库实施顺序

如果要真正落地管理端，数据库建议按以下顺序改造。

### 35.1 第一批：最小可用表

- ✅ `sys_admin_menu`
- ⚠️ `sys_admin_permission`（复用 `sys_permission`，未单独建后台权限表）
- ✅ `sys_admin_role_menu`
- ⚠️ `sys_admin_role_permission`（复用 `sys_role_permission`）

这四张表是动态菜单与权限控制的核心。

### 35.2 第二批：审计与日志表

- ✅ `sys_operation_log`（复用 `sys_audit_log`）
- ✅ `sys_login_log`（复用 `sys_login_audit`）
- ✅ `sys_risk_event`

用于支撑安全、风控和追踪。

### 35.3 第三批：运营与治理表

- ✅ `sys_admin_notice`
- ✅ `sys_banner`
- ✅ `sys_review_task`
- ⚠️ `sys_feedback_reply`（`sys_feedback.reply` 字段）

用于运营中心与审核中心。

### 35.4 第四批：补充表

如有需要，再新增：

- ✅ `sys_admin_setting`（落地为 `sys_runtime_setting`）
- ⚠️ `sys_admin_version`（版本字段在 runtime setting，无独立版本表）
- ❌ `sys_admin_dashboard_snapshot`
- ❌ `sys_admin_statistic_snapshot`

### 35.5 实施原则

- 如果现有表能复用，优先复用。
- 新表必须先确认是否能从现有业务表派生。
- 每张新表都要定义主键、状态字段、审计字段、逻辑删除字段。
- 建表后必须补初始化数据和索引。

---

## 36. 接口实施顺序

为了减少联调阻塞，接口应按下面顺序开发。

### 36.1 第一批接口

1. ✅ `POST /admin/auth/login`
2. ✅ `POST /admin/auth/logout`
3. ✅ `GET /admin/auth/me`
4. ✅ `GET /admin/auth/menus`
5. ✅ `GET /admin/auth/permissions`

### 36.2 第二批接口

1. ✅ `GET /admin/dashboard/summary`
2. ✅ `GET /admin/dashboard/trends`
3. ✅ `GET /admin/users`
4. ✅ `GET /admin/users/{id}`
5. ✅ `GET /admin/roles`
6. ✅ `GET /admin/menus`

### 36.3 第三批接口

1. ✅ 角色授权相关接口
2. ✅ 用户处置相关接口
3. ✅ 操作日志与登录日志接口
4. ✅ 反馈接口
5. ✅ 审核接口

### 36.4 第四批接口

1. ✅ 公告管理
2. ⚠️ 版本管理（settings/client）
3. ✅ 系统配置
4. ✅ 统计接口

### 36.5 接口实施要求

- ✅ 每个接口必须有 Swagger 注解
- ✅ 列表接口必须分页
- ✅ 写接口必须校验参数
- ✅ 写接口必须记录审计日志
- ✅ 失败必须返回统一错误码

---

## 37. 验收标准

管理端第一版的验收标准建议如下。

### 37.1 功能验收

- ✅ 能登录、退出、刷新
- ✅ 菜单按权限动态展示
- ✅ 用户管理可用
- ✅ 角色权限可用
- ✅ 日志可查询
- ✅ 反馈可处理
- ✅ 审核可流转
- ✅ 配置可修改
- ⚠️ 版本可发布（版本信息可配置；非独立发布流水线）

### 37.2 安全验收

- ✅ 未登录不能进入后台
- ✅ 无权限不能访问接口
- ✅ 高危动作要二次确认
- ✅ 所有关键动作进入审计日志
- ✅ Token 过期后会自动退出
- 🔄 生产环境不允许明文密钥（部署验收项）

### 37.3 体验验收

- ✅ 表格分页流畅
- ✅ 详情页信息完整
- ✅ 操作反馈明确
- ✅ 错误提示统一
- ✅ 页面布局一致

### 37.4 联调用例

建议至少覆盖以下场景：

- ✅ 超级管理员登录并查看全部菜单（有 IT/冒烟测试支撑）
- ✅ 运营管理员只看到运营相关菜单（角色种子 + 冒烟）
- ✅ 审核管理员执行封禁、审核与日志查看（角色种子 + 冒烟）
- ✅ 无权限角色无法看到敏感按钮（只读观察员种子）
- ✅ Token 过期自动刷新失败并跳转登录页

---

## 38. 风险点与规避方案

### 38.1 风险一：权限体系重复建设

#### 风险说明

现有后端已经有 RBAC 相关基础，如果管理端再单独做一套权限模型，后续很难统一。

#### 规避方案

- 优先复用现有 `SysRole` / `SysPermission`
- 如需后台专用权限，只在现有体系上扩展，不重新发明模型

### 38.2 风险二：菜单与路由不同步

#### 风险说明

菜单、路由、权限码如果不统一，很容易出现“有菜单没页面”或“有页面没权限”的问题。

#### 规避方案

- 后端返回菜单树时直接附带 `path`、`component`、`permission`
- 前端路由配置与菜单配置同源

### 38.3 风险三：审计日志缺失

#### 风险说明

管理端属于高权限系统，若关键操作没有审计，后续很难排查问题。

#### 规避方案

- 所有写操作统一走审计切面或公共拦截器
- 审计日志字段标准化

### 38.4 风险四：第一版范围过大

#### 风险说明

如果一开始就把风控、BI、审批、配置、统计全部做全，周期会失控。

#### 规避方案

- 第一版只做最小闭环
- 所有高级能力进入第二阶段

---

## 39. 交付物清单

实施过程中建议同步交付这些产物，便于协作和验收。

### 39.1 后端交付物

- 管理端 Controller
- Service
- Mapper
- Entity
- DTO / VO
- Swagger 文档
- 初始化 SQL
- 单元测试

### 39.2 前端交付物

- `linkx-admin` 工程
- 登录页
- 布局框架
- 路由守卫
- 菜单系统
- 用户管理页面
- 角色权限页面
- 日志页面
- 反馈页面

### 39.3 联调交付物

- Apifox / Postman 集合
- 菜单 JSON 样例
- 权限码字典
- 测试账号清单
- 验收 checklist

---

## 40. 最终实施结论

管理端第一版（M1–M4）已按本文档落地：`linkx-admin` 与后端 `/admin/**` 形成可运营闭环。后续按“增强与验收任务”拆解，**不要再从工程初始化或认证骨架重做**。

### 40.1 当前应推进的内容（M5）

- 按角色全链路联调与 §37 验收 checklist 回归
- 生产权限校验、Token/HTTPS/密钥等安全加固
- 风控与审计覆盖核对（写操作、高危动作）
- 对照本文档接口/页面清单查漏补缺（导出、批量、筛选等）

### 40.2 下一步最推荐的开发顺序

1. M5 角色冒烟与权限一致性验收（优先）
2. 生产安全与审计完善
3. 版本强更 / 灰度等运营增强
4. 细粒度数据权限、高级统计等第二版能力

（以下 M1–M4 顺序仅作历史参考，已完成：认证 → 菜单 → 角色权限 → 用户管理 → 日志 → 反馈与审核 → 运营配置 → 统计分析）

### 40.3 这份文档的最终定位

它现在已经可以作为：

- 已交付能力的对照清单
- 技术实施方案与设计依据
- M5 / 第二版排期依据
- 前后端联调与验收依据

如果继续推进，下一步最有价值的是把它拆成三个独立文档：

- 后台接口规格书（与现网实现对齐）
- 后台数据库设计书（与 Flyway 迁移对齐）
- 后台前端实现清单与 M5 缺口清单
