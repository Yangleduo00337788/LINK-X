server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: linkx-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/linkx?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      database: 0

mybatis-flex:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    logic-delete-column: deleted
    print-banner: false

linkx:
  jwt:
    secret: ${JWT_SECRET}
    access-expire: 7200000
    refresh-expire: 604800000
  auth:
    captcha-enabled: ${CAPTCHA_ENABLED:true}
    login-max-attempts: 5
    lock-duration-minutes: 15
    rate-limit-login-per-minute: 10
    rate-limit-register-per-minute: 5
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
  security:
    require-https: ${REQUIRE_HTTPS:false}
  im:
    websocket-port: 8081
