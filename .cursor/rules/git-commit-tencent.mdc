---
description: 每完成一个功能或修复一个 bug 后自动 git commit，使用腾讯中文规范提交信息
alwaysApply: true
---

# Git 提交规范（腾讯中文 Conventional Commits）

## 何时提交

每完成**一个独立功能**或**修复一个独立 bug**后，立即执行一次 `git commit`，不要攒多个无关改动一起提交。

- 同一需求拆成多个可独立验证的小点时：每点完成就提交一次
- 纯探索/未完成的中间态：不要提交
- 用户明确说「先别提交」时：暂停自动提交

## 提交信息格式

```
<type>(<scope>): <中文简述>
```

- 简述用中文，不超过约 50 字，说清「做了什么 / 为了什么」
- `scope` 可选，用小写英文模块名（如 `chat`、`group`、`auth`）
- 正文可选；需要时在空行后补充原因或影响面
- 不要用空洞词（如「更新代码」「修一些东西」）

### type 取值

| type | 含义 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（无行为变化） |
| `perf` | 性能优化 |
| `style` | 格式/样式（不影响逻辑） |
| `docs` | 文档 |
| `test` | 测试 |
| `chore` | 构建、工具、杂项 |
| `ci` | CI 配置 |
| `revert` | 回滚 |

### 示例

```
feat(chat): 支持群精华消息设置与移除
fix(auth): 修复刷新令牌过期后无法重新登录
refactor(group): 抽离群成员角色判断逻辑
chore(cursor): 添加腾讯规范自动提交规则
```

## 执行要求

1. 并行跑 `git status`、`git diff`、`git log` 确认改动与近期风格
2. 只暂存与本次功能/修复相关的文件；排除 `.env`、密钥、`target/`、`dist*`、无意义的 IDE 缓存等
3. 用 HEREDOC 写 commit message（PowerShell 可用 here-string）
4. 提交后 `git status` 确认成功
5. **不要** `git push`，除非用户明确要求
6. 遵守仓库既有 Git 安全约定：不改 git config、不用 `--no-verify`、不 force push main/master、不随意 amend

## 与「用户未要求不提交」的关系

本仓库以本规则为准：**功能完成或 bug 修复完成即应提交**。用户临时禁止提交时除外。
