- 全局状态统一放入 `src/stores/`，禁止在 composable 中使用模块级 ref 管理全局状态
- 组件通信遵循父子 props/emit，跨组件使用 Pinia Store 并通过 `storeToRefs` 读取响应式状态
- 业务实体类型集中定义于 `src/types/index.ts`，开启 TypeScript 严格模式
- 样式优先 UnoCSS 原子类，组件 scoped 样式遵循 BEM 命名，视觉变量统一走 CSS Design Tokens