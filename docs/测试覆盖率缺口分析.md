# LinkX 测试覆盖现状（对齐 2026-07-24）

> 原「缺口分析」已过时。本文描述**当前体系与剩余可选加深项**。

---

## 一、已有测试体系

### 前端
- Vitest 单测 + 覆盖率门禁（最低门槛）
- Playwright E2E：`smoke` + `call-weaknet`
- 脚本：`test` / `test:coverage` / `test:e2e`

前端门禁（最低，非全面覆盖目标）：
- lines 15% / functions 10% / branches 20% / statements 15%

### 后端
- spring-boot-starter-test、H2、embedded redis、JaCoCo、surefire
- 关键模块门禁：行 ≥45% / 指令 ≥40%

---

## 二、已覆盖较好的区域

- 前端 API 层、工具函数、关键组件 mount
- 后端流程测：好友聊天、通话/红包、群邀请、搜索增强、P1/P2 债务与质量门禁
- IM 主链路 / WS Live / 正常链路 / 可靠性 / ExceptionPath（**已纳入 CI 回归子集**）

CI（`.github/workflows/ci.yml`）服务端示例：
`MessageFlowTest, ImMainLinkE2ETest, ImWebSocketLiveE2ETest, WebSocketNormalLinkTest, MessageReliabilityFlowTest, ExceptionPathTest, P1DebtTest, P2*, ChatSearchEnhanceTest, …`

---

## 三、仍可加深（非阻断）

| 方向 | 说明 |
|------|------|
| 前端覆盖率抬升 | 门禁仍是起步值；抬升需单独迭代 |
| 聊天全链路 UI E2E | 需登录态夹具；现 smoke 偏壳层 + 登录字段 |
| UI 复杂交互 | 表情/右键/锁屏等仍偏薄 |
| 真实双端 WebRTC 丢包 | 现有 Fake/CDP + 策略单测 |
| 系统级大群压测工具 | 现有扇出冒烟 |

---

## 四、总结

- **主链路与 WS / 异常 / P1–P2 质量门禁：已具备并可 CI 回归**
- **「仍缺少回执/撤回/搜索」等旧结论作废**
- 后续测试工作以「加深覆盖与真实环境专项」为主，而非补齐空白主能力
